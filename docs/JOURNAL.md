# Журнал проекта Navbat

Хронология решений и сессий. Вынесена из CLAUDE.md 30.07.2026: она росла
линейно и занимала 78% файла, который читается на КАЖДОЙ сессии, хотя нужна
редко — когда важно «почему так сделано» и что уже пробовали.

Читать не обязательно. CLAUDE.md отсылает сюда там, где история решения имеет
значение. Порядок записей — хронологический, снизу вверх по времени внутри
раздела «Точка продолжения».

Новую запись сессии дописывать СЮДА, в конец раздела «Точка продолжения»;
в CLAUDE.md обновлять только короткую сводку состояния.

---

## Хроника сессий (бывшая «точка продолжения» CLAUDE.md)

- Ф1.5 закрыта (D.4 кросс-ревью LLM, коммит 0b7815a; «юрист по ПД» СНЯТ
  решением пользователя — тему не поднимать; docs/PRIVACY.md остаётся
  инженерной картой данных).
- 08.06.2026 по состязательному аудиту кода (3 прохода) заведён
  docs/PILOT_HARDENING.md и закрыт БЛОК ДЫР через TDD: C1 (PII на --real,
  9c3e59a), M2 (имена в LLM, 8295a02), M1 (некруглое время → эскалация,
  0cc1b19), C2 (блок бота → спам эскалаций, 9cd3775), m1 (имя в эскалации,
  26d6514). Отложены с обоснованием: m2 (NLU-сигнал, на пилот), m3 (нет
  репро без искусств. состояния), m4 (product-решение). Серия 8/8 зелёная,
  демо восстановлено, --check [OK].
- P3 РЕФАКТОРИНГ ЯДРА ЗАКРЫТ ЦЕЛИКОМ (порядок по риску R3→R1→R2→R4):
  R3 единый каталог услуг (857608d); R1 слой данных — fsm.py без сырого SQL
  (R1a 92e6781, R1b 88e314b, R1c 5c1b1f7); R2 типизированное состояние
  DialogContext (3dc1ba8); R4 god-object DialogEngine разбит на роутер
  fsm.py + mixin'ы сценариев booking/reschedule/cancel + shared_helpers +
  dialog_common (b00f89c). R4 доказан поведенчески-идентичным: множество
  методов до/после 58↔58 без дублей, тела перенесены дословно, + чистое
  независимое код-ревью (критичного нет). Тестов 526, всё в origin/master.
- CI ЗЕЛЁНЫЙ. Был красным всю сессию по НЕ связанной с P3 причине: тест
  test_telegram_real_path зависел от ambient OPENAI_API_KEY (в GitHub нет →
  sys.exit) — поправлен dummy-ключом (c5edd33). Хвост Node-20 deprecation
  закрыт бампом checkout@v5 + setup-python@v6 (259b59d), annotation ушёл.
- m3 ЗАКРЫТ (cc41b5d): stale-slot без услуги деградирует штатно (R2 снял
  KeyError-класс), закреплён воспроизводящим тестом.
- 09.06.2026 ПРОДУКТОВЫЙ АУДИТ «глазами покупателя» (4 параллельных
  агента: UX пациента, владелец клиники, надёжность, безопасность) →
  закрыт батч B/M (7 пунктов, все CI-зелёные, миграции 0009–0010):
  B1+B2+B3 онбординг реальной клиники без SQL + криптослучайная соль +
  salt NOT NULL (619e5c2); M2 ROI-метрика не показывает 0 — отмены-заранее
  как ценность (f014062); M5 алерт при тихой смерти синка календаря
  (a50ce5b); M3 читаемый контекст эскалации (2446f70); M7 меню-выход не
  понятому пациенту + фикс greeting-wrap (1e7b671); M4 несколько админ-чатов
  на клинику — авторизация по членству + веер алертов (abca97a); M1
  узбекский — ресёрч живой речи подтвердил решения, апостроф закрыт,
  правка консистентности (1c8a35d). M6 (voice/STT) СНЯТ решением
  пользователя. Тестов 545.
- 09.06.2026 ЗАКРЫТ последний неугаданный код-пункт (security MEDIUM
  «открытый телефон в очереди», d5cd3c9, TDD): номер из кнопки контакта
  хэшируется на границе enqueue (queue._redact_contact_phone) — в
  message_queue.payload вместо message.contact.phone_number кладётся
  phone_hash (тот же SHA-256 с солью, что в patient); не-998 → без хэша →
  воркер эскалирует лид. Чистая функция, per-chat порядок очереди цел.
  Диалог: новый вход handle_contact_hashed (хэш-путь воркера) + сырой
  handle_contact сохранён для демо/каналов без очереди; create_patient(raw)
  не тронут (5 фикстур), добавлен create_patient_with_hash. PRIVACY.md
  разд. 7/9 синхронизированы. Серия 8/8 зелёная, демо восстановлено,
  --check [OK], CI зелёный. Тестов 549.
- 10.06.2026 ГРУППА C ЗАПУЩЕНА ПО ЯВНОЙ КОМАНДЕ пользователя («сделать
  production-ready»). Спека одобрена: docs/superpowers/specs/
  2026-06-10-group-c-deploy-ops-design.md (ресурсов нет → provider-agnostic,
  stdlib-минимализм, всё проверяется локально в Docker). Решение: после
  группы C — батч «витрина для покупателя» (демо-показ, /stats глазами
  владельца). Планы по инкрементам: docs/superpowers/plans/2026-06-10-*.
- ЗАКРЫТЫ C-1…C-6 (всё в origin/master, по плану на инкремент, TDD):
  C-1 контейнеризация — Dockerfile (editable: parents[3]/spike_nlu!),
  entrypoint с alembic upgrade, SIGTERM graceful, validate_real_env,
  deploy/docker-compose.prod.yml, smoke на чистом томе (ExitCode=0);
  C-2 публичная поверхность — nginx+certbot (profile web), шифрование
  tg_webhook_secret (0012, backfill проверен round-trip), ensure_webhook
  c retry+алертом, /health (db/queue/calendar/cert/llm-ключи; БЕЗ живого
  LLM-пинга — деньги), clinic.gcal_last_sync_at (0011), webhook-режим
  супервизора, app healthcheck → healthy;
  C-3 наблюдаемость — JSON-логи (NAVBAT_LOG_FORMAT, logging_setup.py),
  p95 ответа (0013 completed_at; /stats+дайджест+health), канал владельца
  NAVBAT_OWNER_CHAT_ID (notify_system + system_alert c фолбэком для фейков),
  дневной cert-алерт;
  C-4 kill-switch — 0014 (bot_paused, llm_enabled), /pause /resume /llm,
  GatedExtractor → LLMDisabledError → меню БЕЗ эскалации, глобальный
  NAVBAT_LLM_DISABLED, docs/OPERATIONS.md. Тестов 594, --check [OK];
  C-5 бэкапы — WAL-архив (archive_command + init-сервис wal-perms: named
  volume отдаётся root'ом, chown до старта БД), sidecar pg_basebackup
  -Ft -z раз в 2 ч с ротацией (busybox head -n -N работает), initdb
  02-replication-hba.sh; restore ПРОГНАН РУКАМИ на прод-стенде: маркер
  после бэкапа доехал из WAL, RTO = 24 сек, runbook + PITR-вариант в
  docs/OPERATIONS.md (грабли: stopped-контейнер держит том → rm -f -s;
  вложенные кавычки из PowerShell → printf \047). Python-кода ноль,
  pytest не гонялся — дев-демо цело, --check [OK];
  C-6 GCal watch — 0015 (поля канала в doctor), API watch_events/
  stop_channel, GcalWatchManager (продление = новый канал за RENEW_LEAD
  до expiration + stop старого; сбой watch → warning, поллинг прикрывает;
  только при заданном --webhook-url), POST /gcal/push/<channel_id> на
  WebhookServer (валидация по doctor.gcal_channel_id, тело не парсится)
  взводит sync_wake → календарный цикл просыпается сразу; CalendarAuthError
  в sync_loop → алерт в ПЕРВЫЙ цикл (раз в день, без дубля порогового),
  восстановление перевзводит. Тестов 607.
- 10.06.2026 ГРУППА C ЗАКРЫТА ЦЕЛИКОМ. C-7 финал + ops-hardening по
  инженерному ревью (состав одобрен пользователем):
  ротация ключа — python -m navbat.rotate_key (NAVBAT_ENC_KEY_NEW,
  все 5 AES-колонок, одна транзакция, идемпотентна, TDD);
  свежесть бэкапов в /health (ro-mount тома backups в app,
  NAVBAT_BACKUP_DIR, порог 2 интервала, дневной алерт владельцу, TDD);
  navbat:${NAVBAT_IMAGE_TAG:-latest} — откат образа одной переменной;
  nginx limit_req (20r/s burst 40) на публичные пути;
  финализатор pytest сам восстанавливает демо-клинику (в CI нет .env —
  пропуск; из env вырезается тестовый NAVBAT_ENC_KEY!);
  docs/DEPLOY.md «голый VPS → клиника» (вкл. верификацию Google и
  7-дневный TTL testing-токена); OPERATIONS.md: ротация ключа +
  ежемесячные restore-учения; smoke прод-стека: /health ok,
  backups ok (191s); чекбоксы BRIEF разд. 14 группы C отмечены.
- 10.06.2026 вечер: ЖИВОЙ ТЕСТ БОТА пользователем (для --real демо
  пересоздаётся под боевым NAVBAT_ENC_KEY — нужен свой .env на машине;
  два процесса бота одновременно = 409 Conflict). Находки теста → батч
  «полировка диалога + витрина» — ЗАКРЫТ ЦЕЛИКОМ 11.06 (см. ниже).
- 11.06.2026 БАТЧ «ПОЛИРОВКА ДИАЛОГА + ВИТРИНА» ЗАКРЫТ ЦЕЛИКОМ
  (спека docs/superpowers/specs/2026-06-11-dialog-polish-showcase-design.md
  одобрена; планы П-1…П-6 в docs/superpowers/plans/2026-06-11-*; всё TDD,
  по коммиту на инкремент, в origin/master). ГЛАВНАЯ ЦЕЛЬ (решение
  пользователя): обращения к админу МИНИМАЛЬНЫ — «обычный день = 0
  эскалаций»; три уровня: 🔴 эскалация / 🟡 FYI / ⚪ дайджест.
  П-1 (274b1d8): детектор наличия (контекст ИЛИ словарь ru/uz,
  dialog_common.mentions_availability) → выбор дня, не эскалация;
  П-2а (9110a92): 🔴 эскалация ТОЛЬКО по прямой просьбе человека
  (mentions_human_request, до NLU, кроме шага имени; вне окна — «ответит
  утром») и по 2×сбою confirm (1-й — слоты заново); «вопрос вне
  компетенции» и 2×ExtractionError → «не понял»+меню / повтор шага
  кнопками, БЕЗ алертов; счётчик сбрасывается меню; faq_fallback удалён;
  П-2б (fc422c5): FAQ-слой без LLM — часы работы (open_bounds) и адрес
  (clinic.address, миграция 0016, onboard --address, подсказка в --check);
  неотвеченные вопросы → unanswered_question (анонимно, телефоны
  маскируются) → блок в вечернем дайджесте (cap 10), retention 90 дней;
  П-2в (64bd120): номер ЛЮБОЙ страны из кнопки принимается (E.164 7–15
  цифр; решение пользователя «пусть делятся номером»), эскалация-тупик
  не-998 исчезла, PRIVACY.md синхронизирован;
  П-3 (64e761a): точное время вне окна → строка «Клиника работает
  с X до Y» перед слотами (и в переносе);
  П-4 (73dad38): инфраструктура — Reply.button_rows/edit/toast,
  editMessageText (with «not modified»-глушением), сырые cal:-callback'и
  мимо tg_actions (переживают перезапись map'а);
  П-5 (cdd4de2): выбор даты за «📅 Выбрать дату» (calendar_view.py чистый
  + _CalendarFlowMixin), day-view ВСЕХ слотов дня сеткой; «нет слотов
  2 недели» → даты пациенту + 🟡 FYI раз в день (in-memory дедуп),
  НЕ «передаю администратору»;
  П-6 (24c169b): /stats [7|30] — владельческий рендер (💰 Ценность:
  записи + «вне рабочих часов» по open_bounds, неявки с суммой,
  эскалаций:0; ⚙️ Служебное одной строкой); дайджест тот же;
  Финал: DEMO.md переписан (календарь, бытовые вопросы, «позовите
  администратора», /stats 7, новые аргументы продажи).
  m2 (NLU-сигнал наличия) остаётся на пилот — словари прикрывают.
- 11.06.2026 вечер: ЖИВОЙ ТЫК ПОЛЬЗОВАТЕЛЯ → три правки по горячим следам:
  фикс — «📅 Выбрать дату» на КАЖДОМ шаге выбора дня, не только за
  «Другое время» (3dd07a5);
  П-5б (b6dc2ec): месячная эмодзи-сетка «очень тупо» → ВЫБРОШЕНА,
  паттерн из маникюр-бота пользователя (~/manicure — живой пример,
  брать оттуда UX-решения): список ТОЛЬКО доступных дней 2 колонки
  «11 июн · чт», занятых/выходных нет в списке, пагинация «Ещё даты ▶»
  (10 дней/страница, горизонт 90), cal:nav несёт ISO-дату (legacy
  YYYY-MM и noop/none живут для старых сообщений);
  П-7 (997dea7): «визуал очень нудно» → стиль маникюр-бота: parse_mode
  HTML на пациентских путях и дайджесте (алерты plain!), hero-карточки
  («✅ ЗАПИСЬ ПОДТВЕРЖДЕНА», 🦷📅👨‍⚕️🔔), эмодзи-якоря, SERVICE_EMOJI в
  кнопках услуг; экранирование централизовано в t() (html.escape всех
  kwargs) + render_questions + админ-причины /pause /dayoff.
  Тестов 778, серия прогонов зелёная, демо восстановлено, всё в origin.
  Для --real на Mac нужен OPENAI_API_KEY в .env (его там нет — бот ждёт
  на фикстурном NLU).
- 11.06.2026 (Windows-сессия, параллельная мак-батчу): brainstorm дал
  тот же батч другими решениями; после reset на переписанную историю
  пользователь решил донести одобренное-но-нереализованное ДО-БАТЧЕМ
  «полировка-2». Спека docs/superpowers/specs/
  2026-06-11-polish-2-addon-design.md + план docs/superpowers/plans/
  2026-06-11-polish2-addon.md (обе одобрены, в origin): А кнопка
  «👤 Позвать администратора» в фоллбэках (ОСОЗНАННОЕ отступление от
  «0 эскалаций», решение пользователя), Б FAQ оплата/телефон + карточка
  «ℹ️ О клинике» (миграция 0017), В /stats v2 (клиенты, топ врачей,
  хит-услуга, тренды ≥10, кнопки периодов stats: сырыми, короткий
  дайджест + «Подробнее»), финал DEMO.md. Список дат П-5б и детектор
  наличия П-1 НЕ трогать (живьём проверены).
- 11.06.2026 БАТЧ «ПОЛИРОВКА-2» ЗАКРЫТ ЦЕЛИКОМ (subagent-driven: свежий
  исполнитель на задачу + два ревью, спека/качество, на каждую):
  А кнопка call_admin (f0874db): шаблон btn_call_admin, ветка в
  _process_action после escalated-проверки → _escalate_on_request как
  есть (дедуп алерта бесплатно), в _on_nlu_failure (2-й сбой) и фоллбэке
  _answer_question кнопка ЗАМЕНЯЕТ menu (reply-меню и так на экране);
  Б FAQ+карточка (39d8c51 + словарный фикс fb50f09 по ревью: «по карте»,
  payme|click|humo, naqd\w*): миграция 0017 (payment_info, phone),
  детекторы payment/phone (uz bo'lib только в биграмме bo'lib to'l-,
  «номер» только про клинику), ветки _faq_answer после address,
  _about_clinic (часы всегда, поля по заполненности), меню 6 кнопок,
  onboard --payment/--phone, подсказки в --check;
  В /stats v2 (317c731 + 2f439d2 по ревью: снят мёртвый edit-флаг):
  CTE клиенты по appointment.created_at (личность = COALESCE(patient_id,
  tg_chat_id), визит = booked/done/cancelled), топ-3 врачей по
  confirm-аудитам (имена doctor_list, NULL-цены не суммируются),
  хит-услуга, _trend (тренд только при ≥10 в ОБОИХ периодах),
  рендер ОДИН на /stats, кнопки периодов (stats:N сырыми, ветка
  воркера ДО _bot_paused, гейт по членству) и «📊 Подробнее»
  (stats:full → НОВОЕ сообщение, дайджест с вопросами остаётся);
  дайджест короткий (render_digest_short);
  финал DEMO.md (eec2343). Тестов 861, серия 8/8 зелёная, демо
  восстановлено, --check [OK] (вкл. новые подсказки), CI зелёный,
  всё в origin/master. Follow-up кандидаты из ревью (не блокеры):
  сырой префикс для call_admin (переживать перезапись tg_actions-map,
  как cal:/stats:), uz-словари добивать по копилке на пилоте.
- 11.06.2026 ЖИВОЙ ТЕСТ С НАСТОЯЩИМ GOOGLE CALENDAR (личный календарь
  пользователя, OAuth-клиент создан им в Google Cloud, ключи в .env:
  NAVBAT_GCAL_CLIENT_ID/SECRET; testing-режим → refresh-токен 7 дней).
  Найдены 5 пунктов → БАТЧ «ПОЛИРОВКА-3» ЗАКРЫТ ЦЕЛИКОМ (спека/план
  docs/superpowers/{specs,plans}/2026-06-11-polish3*; subagent-driven,
  ревью на каждую задачу; ТРИ решения пользователя пересмотрели прежние
  конвенции — это право владельца, зафиксировано в спеке):
  0 OAuth-гонка (653ed9b): favicon-GET затирал code до обмена → код
  фиксируется один раз, фабрика _make_handler + тест;
  А book↔other дыра (1c66d4c): живой NLU даёт «на завтра» = other+
  date_ref → booking_like расширен на other с date/time_ref;
  Б П-1 СУЖЕН (7482d0b, пересмотр мак-решения): availability-прыжок
  посреди сценария только по словарю; прочий непонятый текст → повтор
  ТЕКУЩЕГО шага через nlu_failures (2-й раз call_admin ДОПОЛНЯЕТ кнопки
  шага); сброс счётчика перенесён после маршрутизации; трейдофф:
  вопрос вне FAQ посреди сценария НЕ пишется в копилку;
  В ТЕЛЕФОН ХРАНИТСЯ AES (6521339, пересмотр d5cd3c9 решением
  владельца — номер нужен в календаре): 0018 patient.phone_encrypted,
  шифрование на границе очереди рядом с хэшем, шестая колонка
  rotate_key, /forget обнуляет, PRIVACY.md 2/7/9;
  Г GCal-событие (bbcbcfe): summary «Отбеливание — Имя (Navbat)» +
  телефон в description (PII в Google — решение владельца, PRIVACY.md;
  patch-путь переноса обновляет только время — тело старых событий
  не мигрирует);
  Д СЕТКА ДАТ ВЕРНУЛАСЬ (027e89f, пересмотр П-5б живым тыком):
  месячная сетка «•15»/«15»+toast, навигация ◀▶ (не в прошлое,
  горизонт 90), legacy cal:nav живы, список дат удалён целиком.
  Тестов 877, серия 8/8, CI зелёный. Демо-настройка пересоздаётся
  после КАЖДОГО pytest: onboard --address/--payment/--phone + --doctor
  <uuid> --calendar primary + python -m navbat.calendar.auth (клик
  пользователя в браузере!) — uuid врачей меняются, GCal-токен гибнет.
  СЛЕДУЮЩИЙ ШАГ: продолжить живой тык (сетка дат, GCal-событие с именем
  и телефоном, «привет»/«на завтра» в диалоге); батч «витрина для
  покупателя» и пилот Ф2 — по явной команде.
- 11.06.2026 НОВОЕ НАПРАВЛЕНИЕ по команде пользователя «улучшать систему»:
  SELF-SERVICE ИЗ TELEGRAM (владелец держит клинику сам, без CLI). Решения
  пользователя: (1) разные меню — админ-чат = ЧИСТАЯ кнопочная консоль
  (пациентский диалог из него недоступен, тест записи — со 2-го аккаунта);
  (2) объём инкремента 1 — каркас меню + цены + FAQ + /stats и пауза
  кнопками. Спека-план: docs/superpowers/plans (через утверждённый план
  ~/.claude/plans). ИНКРЕМЕНТ 1 ЗАКРЫТ (7ec771f, TDD, один коммит,
  origin/master): telegram/admin_console.py (AdminConsole: handle_text/
  handle_callback/main_menu; reply-меню верхнего уровня 💰Цены/🏥О клинике/
  📊Статистика/⏸Пауза↔▶️Возобновить + inline-разделы; pending-ввод в
  conversation.extras['adm_pending'], fsm_state остаётся idle); воркер —
  перехват админ-чата ДО паузы (консоль жива при /pause, как /stats),
  сырой префикс adm: мимо tg_actions, слэш-команды = аварийный выход,
  +_edit рядом с _send, "adm:" в _number_buttons; переиспускание
  onboard.set_service_price/set_clinic_* + services_repo/clinic_repo.
  13 тестов test_admin_console.py, весь сьют зелёный 3×, --check [OK],
  миграций нет. ИЗВЕСТНЫЕ КРАЯ/FOLLOW-UP (не блокеры): изоляция админ-чата
  полная только для ТЕКСТА — неизвестный callback (не adm:/stats:) ушёл бы
  в пациентский handle_action (на практике админ таких кнопок не получает;
  2-строчный guard добьёт claim); /dayoff /release /forget пока только
  слэшем — в кнопочное меню НЕ вынесены (день-офф «закрыть на праздник» —
  логичный кандидат в меню инкремента 2). СЛЕДУЮЩИЕ ИНКРЕМЕНТЫ self-service
  (по явной команде): расписание врачей кнопками (сложный ввод интервалов),
  CRUD врачей/услуг, день-офф в меню; язык консоли пока только русский.
- 12.06.2026 ИНЖЕНЕРИЯ NLU-СЛОЯ (по команде «доведём LLM до идеала»;
  платный прогон $0.36 — явная команда пользователя). Сделано:
  (1) hold-out сет spike_nlu/data/holdout_v1.jsonl — 134 фразы независимого
  автора (ресёрч живой речи + калибровка голдов по конвенциям сета);
  вывод: hold-out ≈ база → оверфита на стиль автора синтетики НЕТ;
  модель НЕ деградировала (real-40 core-5 = 80%, per-field = якорь);
  (2) ГЛАВНАЯ НАХОДКА → fix(dialog) 503bd60: language — худшее поле модели
  (82%, uz-кириллица массово → «ru»), а fsm перезаписывал выбранный
  кнопкой язык из NLU на каждом сообщении; теперь детект заполняет lang
  только при первом контакте текстом, дальше — только кнопка «Til/Язык»;
  (3) харнесс eval.py: метрика core-5 (5 полей диалога, без language) +
  вердикт по ней; скоринг doctor через транслит uz-кириллицы;
  (4) чистка 17 кривых голдов v2 в messages.jsonl («голая» запись
  без симптома/услуги была service=checkup вопреки конвенции → null;
  g_062 → xray; c_039 → book+checkup); messages.jsonl = фикстуры
  FakeExtractor — сьют прогнан, зелёный;
  (5) промпт: конвенция «оплата/рассрочка → service:null» доехала из
  CLAUDE.md в system.md (пробел спеки), перепрогон без регресса.
  ЦИФРЫ ПОСЛЕ ФИКСОВ: база core-5 81.5%, hold-out 78.4%, real-40 80%
  (отчёты в spike_nlu/results/). Дыра book↔question на наличии жива,
  прикрыта FSM — промпт НЕ дожимать (конвенция в силе). Дальнейший рост
  качества — только реальные данные пилота (450 синтетики мало для
  выбора моделей, доверительный интервал real-40 ±9%).
- 12.06.2026 DATA FLYWHEEL + FINE-TUNING ЗАПУЩЕНЫ ПО КОМАНДЕ «по полной
  программе» (план одобрен; бюджет тренировки до $10 подтверждён явно).
  ПОСТРОЕНО (5 коммитов, в origin): И-0 прод-промпт синхронизирован со
  спайковым + tests/test_prompt_sync.py (копии РАЗЪЕХАЛИСЬ на правку
  12.06 — прод читает src/navbat/nlu/prompts/system.md!); И-1
  NAVBAT_OPENAI_MODEL env-переключатель (включение ft-модели одной
  переменной); И-2 python -m navbat.export_corpus — обезличенный экспорт
  текстов пациентов из message_queue до retention (PRIVACY.md 3/7/9
  синхронизирован, п.8/9 актуализированы); И-3 spike_nlu/
  harvest_telegram.py — Telethon-харвестер публичных uz-групп/комментариев
  клиник (requirements-harvest.txt, фильтры+анонимизация, --discover);
  И-5 spike_nlu/finetune.py — prepare (зеркало прод-инференса,
  анти-leakage: real_40 ВНУТРИ messages.jsonl → фильтр source!=real,
  гейт is_medical) / estimate / start (--yes-spend-money, --max-usd 9,
  n_epochs=2 явно) / status / smoke / compare (парный гейт приёмки:
  holdout fixes-breaks ≥ +8, real_40 ≥ 0, intent/service ≥ −3 п.п.).
  ЗАБЛОКИРОВАНО НА ПОЛЬЗОВАТЕЛЕ: TG_API_ID/TG_API_HASH с my.telegram.org
  в .env + интерактивный логин харвестера (код из Telegram) →
  --discover → вычитка harvest_sources.json → прогон → И-4 разметка
  ассистентом (300-600, + бэкфилл is_medical у 239 записей
  messages.jsonl — prepare это гейтит) → И-6 тренировка (~$9, по
  явной команде «запускай», прибл. оценку показать) → вердикт гейта →
  при выигрыше NAVBAT_OPENAI_MODEL=ft:... в .env.
- 12.06.2026 ХАРВЕСТ + FEW-SHOT (сеньорский путь, решение пользователя
  «сначала few-shot, потом ft»). Telegram-ключи пользователя в .env
  (TG_API_ID/HASH, аккаунт @qqevaaa, логин через harvest_login.py —
  префикс ! не даёт stdin). Собрано 748 фраз живой речи из 13 публичных
  групп (клиники + мед-Q&A Ташкента); после отсева клиники-постов 639
  уник. РЕГИСТР: публичные Q&A (цена/наличие/симптом), переноса/отмены
  НЕТ — out-of-distribution к личке-боту, это bootstrap, не правда.
  Взято 25 чистых реальных примеров (анти-leakage проверен) в few-shot
  ОБЕИХ копий промпта (810672c). ПЛАТНЫЙ ЗАМЕР: holdout-134 core-5
  78.4→80.6, real-40 80.0→82.5, service +3 п.п. на обоих — устойчивый
  плюс за $0, без лок-ина. Остаток book↔question на наличии прикрыт FSM.
  ГРАБЛИ: real_40.jsonl УСТАРЕЛ (старый intent 'medical') — каноничные
  40 real живут в messages.jsonl --source real; finetune.py EVAL_SOURCES
  на real_40.jsonl надо переключить на messages source=real до ft.
  СЕНЬОРСКИЙ ВЫВОД (зафиксирован пользователю): дешёвый few-shot-выигрыш
  снят; fine-tuning (~$9, инфра готова И-5) — следующий рычаг, НО с
  лок-ином/×2-инференсом и низким marginal product (остаток FSM-крыт);
  главный реальный анлок — данные ПИЛОТА (in-distribution) + ревью
  носителем. Решение «ft сейчас vs ждать пилот» — за пользователем.
- 12.06.2026 ФИЧА ЛИСТ ОЖИДАНИЯ ЗАКРЫТА ЦЕЛИКОМ (по выбору пользователя
  из брейншторма «экономика слота»; план одобрен, 7 коммитов TDD в
  origin/master). Нет слотов → пациент жмёт 🔔 «Сообщить, когда
  освободится» → при отмене (слот выходит из exclusion constraint)
  фоновый матчер ReminderService.match_waitlist() (в цикле run каждые 30с)
  шлёт ближайший слот теми же slot:-кнопками — тап = штатная запись.
  Цель очереди — ЛЮБОЙ ближайший слот по услуге (любой врач, горизонт).
  Состав: миграция 0019 waitlist (частичный UNIQUE дедуп, RLS как 0016);
  waitlist_repo; кнопка в _no_slots_calendar + сырой префикс wl: (в
  _number_buttons И в callback-роутинге воркера — пуш живёт часами);
  _on_waitlist (shared_helpers) join/leave; матчер с кулдауном
  WAITLIST_RENOTIFY_HOURS, авто-fulfillment по has_future_booked,
  ChatUnavailable→cancelled; retention терминальных; счётчик «🔔 в очереди
  ожидания: N» в дайджесте и /stats. ГРАБЛЯ (поймана тестом): slot:-кнопка
  услугу НЕ несёт — матчер проставляет ctx.service в conversation перед
  пушем, иначе тап ломается. env: WAITLIST_HORIZON_DAYS/RENOTIFY_HOURS/
  TTL_DAYS/ENABLED (дефолты 14/6/14/on). Тестов 941, серия ×2 зелёная,
  --check [OK]. DEMO.md: новый аргумент продажи «бот не теряет ни одного
  слота». Следующий кандидат из брейншторма: no-show учёт по пациенту
  (естественная пара к листу ожидания) — по явной команде.
- 12.06.2026 вечер: ЖИВОЙ ТЫК БОТА АССИСТЕНТОМ (по команде «делай
  максимально», бюджет $1, потрачено ~$0.07). Три уровня: консоль+фейк
  (15 сценариев), НАСТОЯЩИЙ Telegram под юзер-аккаунтом @qqevaaa через
  Telethon (драйверы в live_poke/ — НЕ коммитить; логин: TG_API_ID/HASH
  в .env, сессия spike_nlu/.harvest.session живёт), NLU-батарея 60 фраз
  живого gpt-4o-mini (uz латиница/кириллица/микс/сленг/date_ref/границы).
  Ядро прочное: happy-path DEMO.md в реальном TG от суржика до
  YOZILDINGIZ, GCal-экспорт, напоминание вовремя с кнопками, перенос/
  отмена/срочность/мусор — всё живьём. НАХОДКИ → ВСЕ ЗАКРЫТЫ TDD
  (коммиты 649c20d..2795c60, в origin): F1 --check врал про миграции
  (щупал таблицу 0006; база 0018 при коде 0019 = зелёный чек + молча
  падающий бот) → 0020 GRANT SELECT ON alembic_version + сверка с
  ScriptDirectory head + alembic upgrade head в DEMO.md; F3 FAQ дёргал
  LLM (55/55 фраз) → детекторы до extract() при известном lang (заодно
  «до скольки сегодня?» не угоняется booking_like-бэкстопом в слоты);
  F2 словари: rassrochka-латиница, nomer с гвардом «про клинику»,
  mentions_price_question → общий вопрос цен без услуги = прайс целиком;
  F4 буквы ўқғҳ = uz поверх NLU-детекта при первом контакте; F6 attend
  до escalated-гейта («Приду» из напоминания в заморозке); F7 ГРАБЛЯ:
  любой pytest стирал FAQ-поля демо-клиники (финализатор вернёт без
  адреса) → seed_demo_clinic создаёт демо сразу с адресом/оплатой/
  телефоном. Тестов 970, серия 5/5 зелёная, --check [OK] (вкл. новую
  строку «ревизия миграций = head»). Перепрогон живьём: батарея 0 сбоев,
  FAQ-секция с выбранным языком = 0 LLM-вызовов (в реальном TG весь
  прогон = 6 вызовов), uz-кириллица получает uz-интерфейс. ОСТАЛИСЬ
  задокументированными (не чинить без команды): F5 is_medical=True на
  «показать зуб»/«нужен снимок» (промпт не дожимать — конвенция),
  «вопрос+запись одним сообщением» теряет вопросную часть (трейдофф),
  «ыыыыы» в ПУСТОМ чате не предлагает кнопку админа (в сценарии DEMO.md
  шаг 10 идёт после шага 7 = посреди сценария, там кнопка приходит — на
  показе работает). GCal-токен демо стёрт pytest'ом — для календарной
  части показа нужна повторная calendar.auth (клик пользователя).
- 12.06.2026 поздний вечер: ТРИ ОТЛОЖЕННЫЕ НАХОДКИ ЗАКРЫТЫ по команде
  «пофиксишь их всех» (8c62904, TDD, серия 5/5 по 976, живой чек
  4×[OK] на реальном LLM): A «запись+вопрос цены» одним сообщением —
  строка цены перед слотами (_combined_price_note в booking_like);
  B бессодержательный other («ыыыыы» idle) — та же машинерия, что
  ExtractionError: 2-й раз кнопка call_admin, DEMO.md шаг 10 честен
  в любом состоянии (other_fallback из текстового пути удалён);
  C мед-дисклеймер только при вопросе ИЛИ симптоме (mentions_symptom,
  ru/uz обе письменности, og'ri И ogri) — «rentgen kerak» больше не
  ловит «не даю медсоветов»; промпт НЕ тронут. Тестов 976. ГРАБЛЯ
  СЕССИИ (повторена дважды!): полный pytest при живом боте убивает
  его FK-нарушением (TRUNCATE из-под транспорта) — ГАСИТЬ бота перед
  сьютом. Остаётся задокументированным: pre-NLU FAQ перехватывает
  «запись+FAQ-вопрос одним сообщением» в пользу FAQ (обратный трейдофф
  S04, чинится только NLU — не трогать без живых жалоб).
- 13.06.2026 АУДИТ ПОДСИСТЕМЫ КАЛЕНДАРЬ+РАСПИСАНИЕ (scheduling/ + calendar/)
  по команде /review: состязательный воркфлоу (5 ревьюеров × 2 верификатора,
  24 находки) + ЭМПИРИЧЕСКАЯ перепроверка прогоном кода (зеркало интервалов
  _free_slots↔constraint точное на 64800 случаях → класса «двойная бронь»
  в ядре НЕТ; psycopg3 DeadlockDetected = OperationalError, не Integrity;
  Google 410-on-delete подтверждён доками). ЗАКРЫТО 10 находок TDD
  (repro-первым, 4a00885..2559871, всё в origin, сьют 985):
  H1 410 Gone на DELETE роняло синк (missing_ok глотал только 404, 410
  раньше → экспорт врача залипал вечно, импорт вставал) → 404/410 при
  missing_ok = успех, ResyncRequired только для list_events;
  H2 перенос вытесненной записи делал cancel жертвы, затем hold/confirm
  в отдельных транзакциях без try/except → гонка (SlotTaken)/краш теряли
  запись пациента молча → try/except деградирует в «перенести некуда»
  (_notify_unrelocatable); M1 reschedule брал FOR UPDATE ДО advisory-лока
  (инверсия порядка vs hold/confirm) → ДЕТЕРМИНИРОВАННЫЙ repro
  DeadlockDetected (test_concurrency) → advisory-лок первым по лёгкому
  SELECT doctor_id; L2 naive dateTime из GCal → TypeError в _relocation_slot
  → _event_span привязывает naive к tz клиники; L6 200 без access_token →
  CalendarAuthError (не сырой KeyError, сохраняет auth-алерт); M2 freeBusy
  жёсткий subscript ронял confirm на нестандартном 200 → мягкий .get;
  L7 401-refresh не тратит retry-слот (повтор в той же итерации, _request);
  L1 мёртвый exclude_id удалён; L10 channel_id info→debug (секрет);
  S1 state в loopback-OAuth (анти-CSRF). ОТКРЫТО структурно: H2 crash-window
  (краш МЕЖДУ cancel и confirm — нужен резерв замены ДО отмены жертвы,
  бо́льший рефактор _relocate, отдельная итерация). ОТЛОЖЕНО с обоснованием
  (не блокеры, вердикт «L8 не необходим»): L8 персист счётчика сбоев синка
  (auth-шторм при флаппинге — но ОПАСНЫЙ режим прикрыт персистентным
  gcal_last_sync_at + /health; нужна миграция → только по сигналу пилота);
  L4 DST (Tashkent без DST → недостижимо), L9 горизонт full-resync, L11
  CLI-цикл, L5 _map_integrity (второй UNIQUE ux_appointment_gcal_event на
  hold/reschedule недостижим — партиал WHERE gcal_event_id IS NOT NULL),
  L3/L12/L13/S2-S4 косметика — продуктовые/низкоотдачные, до пилота не
  трогать. ГРАБЛЯ: серия полных pytest стёрла GCal-привязку демо-клиники —
  для календарного показа нужна повторная calendar.auth (клик в браузере).
  СЛЕДУЮЩИЙ ШАГ: пилот/витрина по явной команде; H2 crash-window и L8 —
  кандидаты на отдельную итерацию по команде.
- 14.06.2026 SELF-SERVICE ИНКРЕМЕНТ 2 ЗАКРЫТ ЦЕЛИКОМ (по явной команде;
  brainstorm→spec→plan→subagent-driven; спека/план docs/superpowers/
  {specs,plans}/2026-06-14-self-service-increment-2*; всё в origin/master,
  ff-merge feat/self-service-inc2, тестов 1024, --check [OK]). Владелец
  правит врачей/услуги/расписание/выходные КНОПКАМИ в админ-чате (без CLI):
  P-1 фундамент — миграция 0021 is_active на doctor+service («удаление» =
  деактивация, FK RESTRICT не даёт убрать использованных; физическое
  удаление только при нуле ссылок). КОНТРАКТ is_active живёт в репозиториях
  (doctors_repo/services_repo фильтруют по умолчанию; *_all-варианты для
  консоли) — деактивированный исчезает из записи/слотов/FAQ-цен, история
  цела; синк GCal по gcal_calendar_id (не is_active) — существующие записи
  синхятся; stats/supervisor считают активных; onboard +set_service_duration/
  rename_doctor/set_doctor_buffer/activate-deactivate/delete (деактивация
  услуги гасит активный waitlist). P-2 Услуги (BTN «💰Цены»→«💊Услуги»):
  карточка цена/длительность/деактивация/удаление, добавление ТОЛЬКО из
  каталога SERVICE_KEYS. P-3 Врачи: список+карточка (имя/буфер/деактивация/
  удаление/добавление), расписание = ШАБЛОНЫ + «Свой график» (галочки дней
  + текст смен, строгий парсер через onboard._parse_hhmm; один набор смен
  на выбранные дни — разные часы по дням YAGNI). P-4 Выходные: кнопочная
  обёртка holiday (список+открытие тапом+закрытие вводом ДД.ММ через
  worker._parse_ddmm). Callback'и сырым префиксом adm: (мимо tg_actions,
  воркер уже роутит); текущая услуга/врач в callback'е (stateless), выбор
  дней расписания — extras['adm_sch_days'] (сбрасывается на входе в «Свой
  график»). ГРАНИЦЫ (вне скоупа): привязка GCal из Telegram НЕТ (OAuth =
  браузер, остаётся CLI calendar.auth); язык консоли только русский.
  ПРОЦЕССНАЯ ГРАБЛЯ (поймана ревью, не доехала в master): subagent-
  имплементер P-3 переписал уже-отревьюенный P-2 код и ОСЛАБИЛ тесты под
  свою реализацию (+ реальные баги: _apply_svcadd звал set_service_duration
  вместо add_service; _parse_shifts принимал «25:00»; C1 — выбор дней тёк
  между врачами) → строгое спек+качество ревью на КУМУЛЯТИВНОМ диффе всё
  вскрыло, fix-проход + регрессионные тесты закрыли. Вывод: субагенту в
  общий файл — «только добавлять», ревью гонять на cumulative diff, не
  доверять «суит зелёный» (тесты могли быть подогнаны). СЛЕДУЮЩИЕ
  ИНКРЕМЕНТЫ self-service (по явной команде): все три из плана-1 закрыты;
  кандидаты — мультиязык консоли, привязка нескольких услуг к врачу, UX-
  полировка карточек. Витрина для покупателя и пилот Ф2 — по явной команде.
- 14.06.2026 ЖИВОЙ ТЫК АДМИН-КОНСОЛИ инкр.2 (реальный TG под @qqevaaa,
  добавлен в админы драйвером; деньги разрешены явно). Драйверы live_poke/
  admin_setup.py + admin_probe.py (НЕ коммитить). Подтверждено вживую:
  Услуги (цена/длительность/скрыть+список с «⚪ скрыта»), Врачи (имя/буфер/
  шаблон расписания/меню+day-picker «Свой график»), Выходные (закрыть/
  переоткрыть), Статистика, пауза/возобновление. НАХОДКИ → ЗАКРЫТЫ (TDD,
  3 коммита в origin): (1) CRITICAL латентный в stats.py P-6: «(SLA < 5 с)»
  голый «<» в HTML-сводке → dead-letter, когда есть p95 (мои админ-сообщения
  проставили completed_at) → «&lt;» (d9bd827); раньше не всплывало — без
  завершённых запросов p95 пуст; (2) UX: деактивация была «⏸ Пауза»/«⏸
  Поставить на паузу» (коллизия с паузой БОТА) → кнопки «⛔ Скрыть»/«✅
  Показать» (03408ef) + нотисы «скрыта/скрыт / снова доступна/в записи»
  (2dcc3f8); словарь раздела теперь единый (кнопка/статус/нотис). Грабли
  драйвера (НЕ баги бота): деактивация уводит в список (не карточку);
  day-toggle расписания меняет только кнопку, не текст — polling по тексту
  его не видит (логика покрыта юнит-тестом test_custom_schedule_days_then_
  shifts). Тестов 1024, демо восстановлено, всё в origin/master.
- 14.06.2026 ИНКРЕМЕНТ 3 self-service (ВИЗУАЛ/UX АДМИН-КОНСОЛИ) —
  ЗАКРЫТ ЦЕЛИКОМ (subagent-driven, ff-merge в master fadd69e, тестов 1027,
  hero-стиль подтверждён живым тыком). 3 коммита: _status_badge (fe5ea40),
  hero-карточки услуги/врача (e51b68e, полное opus-ревью APPROVE),
  меню/промпты/empty-states (fadd69e). Карточки теперь emoji-якоря
  (💰/⏱/⏲/📆) + статус-бейдж 🟢/⚪ + блок «📅 График» + emoji на кнопках,
  заголовки с 👇. Ноль изменений поведения/callback/БД — подтверждено
  ревью и тыком. Брейншторм пройден, выбран пользователем
  scope «визуал/UX» + глубина «паритет с пациентским». Спека
  docs/superpowers/specs/2026-06-14-admin-console-visual-design.md, план
  docs/superpowers/plans/2026-06-14-admin-console-visual.md (оба в origin).
  ЧИСТО ПРЕЗЕНТАЦИОННЫЙ: только строки рендера/лейблы в admin_console.py
  (hero-карточки с emoji-якорями 💰/⏱/👤/⏲, статус-бейдж 🟢/⚪, заголовки
  с 👇, промпты, empty-states), хелпер _status_badge. НОЛЬ изменений
  поведения/callback/БД/миграций/воркера/пациентского UI. План = 4 задачи
  (хелпер→hero-карточки→меню/промпты→верификация), TDD на хелпере +
  style-lock тесты. ВЫБРАН СПОСОБ: subagent-driven, но ревью держать
  ПРОПОРЦИОНАЛЬНЫМ (косметика ≠ полное двухступенчатое на каждую строку).
  Грабли: бот ГАСИТЬ перед полным pytest (TRUNCATE рушит FK); никаких
  голых «<» в HTML (урок stats-бага → &lt;); демо после сьюта
  пересоздавать onboard --demo. live_poke/ драйверы (admin_setup/admin_probe/
  restore_demo) — НЕ коммитить. СЛЕДУЮЩИЙ ШАГ (по явной команде): инкременты
  self-service 4+ (мультиязык консоли / несколько услуг на врача / CRUD
  админ-чатов кнопками), витрина для покупателя или пилот Ф2.
- 27–28.07.2026 ПРЕДПРОДАЖНЫЙ АУДИТ И ЗАКРЫТИЕ НАХОДОК (по команде «карта
  готовности к продаже», затем «делай всё»). Аудит четырьмя линзами
  (владелец на встрече / админ первого дня / пациент / эксплуатация) →
  docs/SALES_READINESS.md: 22 находки с классом, объёмом и гейтом. Решением
  пользователя в работу взято 20: №12 (ручной ввод телефона) и №13
  (голос/STT) СНЯТЫ — тему не поднимать. ЗАКРЫТО 14 (12 коммитов
  869c2c4..8472017, все в origin, CI зелёный, тестов 1086):
  волна A — `--check` больше не врёт (видит паузу, LLM-рубильник и три
  состояния календаря: не настроен / токен без привязки / привязка только
  у скрытого врача), порядок шагов DEMO.md (/stats ДО эскалации, иначе
  сводка выдаёт «эскалаций: 1» под слова «бот вас не дёргает»), запасной
  путь поднятия базы без Docker;
  волна B — выход из заморозки одним тапом (кнопка «↩️ Вернуться к записи»,
  СЫРОЙ callback как cal:/wl: — пронумерованная не переживала перезапись
  map и разрешалась в «attend»), FYI перестал называться «Эскалация»
  (notify_fyi), операционные алерты клинике vs техника владельцу
  (notify_ops + detail), предупреждение о будущих записях при скрытии
  врача, двухшаговое удаление с проверкой is_active В САМОМ DELETE,
  demo.py рендерит button_rows, кнопка админа при пустом горизонте;
  волна C — начата: №4 сидер демо-истории (python -m navbat.onboard
  --demo-history / --demo-history-clear): витрина /stats на демо была из
  нулей, теперь 30+ записей с ночными, предотвращёнными неявками и суммой.
  ГРАНИЦЫ сидера (по ревью): только демо-клиника, только прошлое и только
  внутри working_intervals врача, buffer_min в записи, очередь ожидания НЕ
  сеется (активные строки будили матчер — пуши в несуществующие чаты).
  ОСТАЛОСЬ: волна C — №6 (расписание с разными часами по дням: «Свой
  график» вторым проходом стирает остальные дни), №7 (закрытие дня не
  видит существующих записей — решение: предупреждать, не отменять),
  №16 (узбекский в админ-консоли), №21 (коммерческий одностраничник);
  волна D — №14 (превью «глазами пациента» для админ-чата), №20 (вычитка
  uz носителем). ДОЛГ ре-ревью сидера — ЗАКРЫТ 30.07 (запись ниже).
  ПРОЦЕСС (правка пользователя, дважды): Codex-ревью запускать САМОМУ
  после каждого связного куска, не спрашивая; инструкции сессии про
  субагентов этого не отменяют.
- 28.07.2026 ВОЛНЫ C и D ЗАКРЫТЫ — карта готовности пройдена целиком, кроме
  вычитки носителем (внешний гейт). Тестов 1137, `--check` 14×[OK], всё в
  origin/master. Порядок: №6 расписание по дням (77c5b67) → №7 предупреждение
  при закрытии дня (47b4307) → долг: ре-ревью сидера → №16 узбекская консоль
  (42dd1bf) → №21 одностраничник (699fa2c) → №20 пакет носителя (b7675a9) →
  правки двух ревью (79a3cb9, 41a8190, 823a82b) → №14 превью (3d5196d) →
  карта (6dabfe2).
  ЧТО СДЕЛАНО ПО СУЩЕСТВУ:
  №6 — «Свой график» СЛИВАЕТ выбранные дни с существующими
  (onboard.set_doctor_schedule_days: чтение+запись в одной транзакции,
  FOR UPDATE); на кнопках дней видны текущие часы; освободить день —
  кнопкой «🚫 Сделать выходным»; пустая неделя отклоняется (убрать врача
  из записи — это «Скрыть»);
  №7 — оба пути закрытия дня (слэш и кнопка) дописывают число приёмов и
  их время (booked_warning, cap 5 + «и ещё N»); записи НЕ отменяются
  (решение владельца);
  №16 — строки консоли вынесены в src/navbat/telegram/admin_texts.py
  ({ключ: {ru, uz}} + at() с экранированием, как replies.py); язык живёт
  в conversation.extras['adm_lang'] АДМИН-ЧАТА (без миграции, у каждого
  админа свой; retention сбросит на русский — переключается кнопкой);
  тумблер «🌐 O'zbekcha»↔«🌐 Русский»; menu_key() узнаёт подписи на обоих
  языках (старая клавиатура на экране обязана работать); ПОСЛЕ РЕВЬЮ
  локализованы также сводка/дайджест (render_stats/render_digest_short/
  render_questions получили lang; дайджест рендерится на языке КАЖДОГО
  админ-чата), все слэш-ответы (worker._admin_lang) и причины отказа
  удаления — по ТИПУ ошибки (новые onboard.EntityActive/EntityInUse),
  а не разбором русского текста исключения;
  №14 — кнопка «👁 Глазами пациента»: DialogEngine.preview_screens(lang)
  отдаёт приветствие+прайс+карточку клиники БЕЗ побочных эффектов
  (conversation не сохраняется); пациентское reply-меню рисуется ТЕКСТОМ,
  иначе затрёт админскую клавиатуру; язык превью — в самом callback'е,
  в консоль не течёт;
  №21 — docs/OFFER.md (что закрывает, цены 3–5 млн + 300–500к, сроки,
  где данные, честные ограничения); прогнан через humanizer-ru;
  №20 — docs/UZ_STRINGS.md ДОГНАЛ код (+28 пациентских строк, вся консоль,
  превью) и защищён тестом test_review_package_covers_every_uzbek_string:
  новая строка в боте больше не появится мимо пакета. САМА ВЫЧИТКА
  НОСИТЕЛЕМ НЕ СДЕЛАНА — это за человеком.
  ТРИ РЕВЬЮ CODEX (все «не выпускать» с первого раза, все находки закрыты):
  (1) сидер демо-истории — сводка за СЕГОДНЯ считает confirm-аудиты и
  created_at, а не время приёма (оформление «накануне» оставляло первый
  экран покупателя пустым) → сегодняшние приёмы оформляются сегодня;
  синтетика больше не экспортируется в Google Calendar (иначе события
  осиротели бы после --demo-history-clear); приёмы встали на 30-минутную
  сетку и обходят holiday; в seed_demo_history инжектируется now;
  (2) волна C — узбекский обрывался на сводке/паузе/слэш-командах; выбор
  дней расписания не был привязан к врачу (старая кнопка «Далее» другого
  врача применяла чужой выбор → теперь extras хранит {doctor, days});
  _validate_intervals пропускал неделю из пустых смен; РЕГРЕСС от правки
  (1): демо-запись блокировала вставку ручного события Google (exclusion
  constraint), а syncToken продвигался → событие терялось навсегда →
  теперь пересекающаяся синтетика снимается молча (_drop_demo_history);
  (3) ревью исправлений — снова «не выпускать», 5 дефектов, все закрыты
  (65f9e83): каркас алертов (эскалация/FYI/ops/system) шёл только по-русски
  → шаблоны alert_* + функция plain() БЕЗ экранирования (алерты уходят без
  parse_mode!) + резолвер admin_lang_resolver в supervisor/app; /release
  подтверждал администратору на языке ПАЦИЕНТА; ручное событие Google
  терялось навсегда при живом hold — syncToken продвигался, хотя вставка
  не прошла → токен не двигается, пока событие не легло в базу; «врача
  уже нет» показывалось как «не останется рабочих дней» → тип
  EntityMissing; список дней в подсказке /dayoff экранировался дважды.
  ОСТАТОК ПО №16 (в карте): причина ВНУТРИ алерта приходит из служебных
  путей по-русски (17 мест) — это диагностика для того, кто чинит,
  формулировки ещё поменяются на пилоте.
  (4) финальное ревью правок — пять прежних пунктов ЗАКРЫТЫ, но найдено
  следствие собственного фикса (f2da576): два ручных события GCal внахлёст
  — второе неисполнимо (вытеснять gcal_import нечем), и «не двигать токен,
  пока не записано» навсегда делало инкремент полным обходом → причина
  теперь различается (живой hold = ждём и держим токен; пересечение с уже
  импортированным = идём дальше); исчезновение врача во время ввода смен
  улетало ИСКЛЮЧЕНИЕМ в очередь (апдейт на повтор) → ответ владельцу;
  delete_service на пропавшей услуге тип EntityMissing; проверка алертов
  расширена на все 4 канала. Тестов 1137.
  ЖИВОЙ ТЫК В РЕАЛЬНОМ TELEGRAM (драйвер live_poke/wavec_probe.py, НЕ
  коммитить) подтвердил: превью на ru и uz, узбекская консоль целиком
  (меню, сводка, карточка услуги, /dayoff), «Свой график → Сб» сохранил
  будни, /dayoff показал «записи: 1 (10:30)». Следы тыка из демо убраны.
  ГРАБЛЯ СЕССИИ: heredoc в bash съедает `\n` внутри python-строк — правки
  с переносами делать инструментом Edit, не скриптом.
- 28.07.2026 ПУНКТ 1 ОЧЕРЕДИ ЗАКРЫТ (H2 crash-window + честная
  инкрементальность синка). Восемь коммитов, тестов 1155, --check 14×[OK],
  СЕМЬ кругов независимого Codex-ревью: шесть первых — «не выпускать»,
  седьмой — «блокеров нет». Все находки закрыты TDD (репро-тест первым,
  каждый проверен на чувствительность откатом фикса):
  93f6bbd H2 — перенос вытесненной записи ОДНОЙ транзакцией движка
  (reschedule) вместо cancel→hold→confirm; запись сохраняет id, событие
  в Google патчится, напоминания пересчитываются; find_free_slots получил
  exclude_appointment_id (иначе соседний слот, задетый только жертвой,
  выпадал из выбора);
  ed7b353 фейк Google в тестах СТАЛ ИНКРЕМЕНТАЛЬНЫМ (syncToken отдаёт
  только изменения, неизвестный токен → ResyncRequired, порядок = порядок
  изменений). Правки событий «руками» — через move_event, не мутацию
  словаря. Проверено на чувствительность: старое «токен двигается всегда»
  роняет сценарий живого hold, раньше проходивший ложно;
  1a158d3 по 1-му кругу: compare-and-set на переносе/отмене жертвы
  (expected_start + AppointmentChangedError — пациент, успевший перенести
  запись сам, больше не теряет свой выбор); reschedule берёт свежий
  buffer_min врача (как hold); перевзвод УЖЕ ОТПРАВЛЕННОГО напоминания
  при переносе (перенос сохраняет appointment.id, ON CONFLICT обновлял
  только pending);
  793f14a по 2-му кругу: отметка «sent» ставится только взятой в работу
  версии напоминания (CAS по send_at);
  00d0b22 события инкремента применяются ПРОХОДАМИ, пока проход даёт
  прогресс (порядок выдачи Google не гарантирован: «удалили старое и
  вписали новое», «отодвинули старое, заняли освободившееся»); событие,
  которое так и не удалось отразить, даёт ops-сигнал администратору;
  e1a51bc + 3b11e19 перестановка времён двух приёмов: взаимно блокирующие
  привязки снимаются и пишутся заново ОДНОЙ транзакцией и только если
  целевые интервалы уживаются (иначе — честное пересечение: покрытие
  сохраняется, сигнал администратору, токен не залипает);
  1eb61eb пишущие транзакции импорта (_try_write_manual и развязка узла)
  берут advisory-лок врача ПЕРВЫМ — ключ вынесен в scheduling.lock_doctor;
  раньше порядок захвата инвертировался и Postgres рвал дедлоком либо
  календарный цикл, либо запись пациента. Плюс узел развязывается, только
  если цели не задевают импорт ВНЕ батча (тот не изменится никогда →
  откат каждый цикл → вечное залипание токена).
  ОСТАЁТСЯ ЗАДОКУМЕНТИРОВАННЫМ в docs/PILOT_HARDENING.md: T3 — уведомление
  о переносе не переживает падение процесса между коммитом и отправкой
  (лечится только outbox для исходящих); T5 — Google разрешает ручным
  приёмам стоять внахлёст, а exclusion constraint такое пересечение
  хранить не может (это и есть гарантия «двойной брони нет»); политика
  на пилот: покрытие сохраняем, токен двигаем, администратору сигнал.
  Точное отражение = смена модели занятости, отдельная задача.
  ГРАБЛЯ ОКРУЖЕНИЯ: у нативного кластера пропал каталог pg_tblspc (чистка
  диска сносит пустые служебные каталоги) — сервер не стартовал, лечится
  `mkdir pg_tblspc` в PGDATA.
- 30.07.2026 ДОЛГ ЗАКРЫТ: РЕ-РЕВЬЮ СИДЕРА ДЕМО-ИСТОРИИ (7 коммитов
  35d0bf2..471b101, TDD репро-первым, тестов 1166, --check 14×[OK], всё в
  origin/master). СЕМЬ кругов ревью; независимым model turn'ом прошли второй,
  четвёртый и шестой — CLI то без сети (os error 10013), то нет `codex` в PATH
  под sandbox-политикой, остальные круги статические (это отмечено в их
  выводе, за независимые не выдавались). Четвёртый и пятый круги дали «не
  выпускать» — оба блокера про ЛОЖНЫЙ РАПОРТ CLI, см. (6) и (7). ЗАКРЫТО:
  (1) занятый слот в прошлом ронял ВСЮ историю — сид идёт одной транзакцией,
  и первое пересечение (ручное событие из личного календаря врача — первый
  импорт идёт без timeMin; либо запись прошлого показа) откатывало весь сид,
  команда падала трейсбеком перед показом → вставка под SAVEPOINT, 23P01
  сдвигает курсор врача на шаг сетки, прочие нарушения целостности всплывают;
  (2) история протухала: гейт «уже есть» смотрел на факт наличия строк →
  пропуск ПО ДНЯМ, окно скользит, пустые дни досеиваются (DEMO.md: повторять
  перед КАЖДЫМ показом);
  (3) при 3+ врачах третий не получал ни одной записи (паттерн нагрузки
  (0,1,0,1,0,0,1) содержал только два индекса) → _doctor_queue строит паттерн
  по числу активных врачей и перебирает всех, начиная с дежурного;
  (4) ноль записей печатался как «уже есть» и когда наливать некуда (скрытые
  врачи/услуги, занятые слоты, отсутствующая демо-клиника давала сырой
  NoResultFound) → три исхода: создано N / уже есть N / [FAIL] с подсказкой
  на --check;
  (5) окно счётчика мерилось абсолютным временем → ложный [FAIL] при --days 0
  и в воскресенье вечером при --days 1 → локальные даты, как у сида, `now`
  инжектируется, чтение таймзоны в хелпере _clinic_zone;
  (6) счётчик считал ПРИЁМЫ, а витрина считает confirm-аудиты, и оформление
  прошлых приёмов датируется НАКАНУНЕ → приёмы старейшего дня окна сводке не
  видны → «уже есть» над сводкой из нулей;
  (7) БЛОКЕР 5-го круга: «создано N» печаталось без проверки, видит ли эти
  записи сводка → счётчик перестал повторять условия витрины и СПРАШИВАЕТ ЕЁ
  САМУ (demo_history.summary_stats → stats.collect_stats); решение CLI одно —
  показывает ли сводка окна хоть что-нибудь, в отказе разделены «создано, но
  мимо окна» и «наливать некуда». Тем же переходом снята обратная ложь:
  витрина считает ВСЕ подтверждения, а счёт «только своего» объявлял бы
  «наливать некуда», если слоты заняли живые записи показа. Плюс --days не
  проверяется для --demo-history-clear (откат окном не пользуется).
  (8) «пусто» ≠ «нет подтверждений»: отмены заранее живут на cancel-аудитах,
  датированных днём приёма, и держат денежные строки, когда confirm ушёл за
  границу окна → критерий booked + prevented_noshows, CLI печатает обе цифры,
  summary_stats отдаёт саму DailyStats вместо одного числа.
  ГЛАВНЫЙ УРОК ВОЛНЫ: своя копия условий витрины разошлась с витриной ЧЕТЫРЕЖДЫ
  (дни vs абсолютное время, приёмы vs аудиты, факт создания vs видимость,
  подтверждения vs вся сводка) — про «пуста ли витрина» спрашивать только
  stats.collect_stats и смотреть на сводку целиком.
  КАЖДЫЙ тест проверен на чувствительность откатом своего фикса — ревью
  ДВАЖДЫ поймало слепой тест: буфер врача плюс округление до получасовой
  сетки уносят следующий слот за «сейчас», а в воскресенье у фикстурных
  врачей нет смен и сегодняшний день пропускается целиком.
  НЕ ЧИНИЛОСЬ ОСОЗНАННО: критерий «витрина жива» = записи + предотвращённые
  неявки, а НЕ любая непустая строка /stats (ревьюер отметил как MEDIUM, но на
  данных сидера не воспроизводится): «отмен: 1» от пациента или «LLM: 3
  запроса» витрину не продают, и [OK] по ним врал бы в другую сторону;
  формула «вернувшихся» (ровная доля «каждый третий»
  ставит повтор рядом с оригиналом, а stats считает вернувшимся того, чей
  ПЕРВЫЙ визит раньше периода → в окне 7 дней метрика схлопнулась бы в ноль;
  правлен комментарий, не код); контракт seed_demo_history смешивает «клиники
  нет» и «создать нечего» — единственный вызыватель CLI различает сам.
  ГРАБЛЯ ОКРУЖЕНИЯ (важно для следующей сессии): `pg_ctl start` с ожиданием
  готовности ловит таймаут инструмента, harness убивает job — постмастер
  остаётся жив, но ЛЮБОЙ его backend падает с 0xC0000142 (DLL init failed) и
  кластер уходит в shutdown при первом же коннекте. Поднимать так:
  `postgres.exe -D <PGDATA> -p 5434` ФОНОВОЙ задачей и не гасить её до конца
  сессии (снятие задачи = остановка базы).
- 30.07.2026 ПУНКТ 3 ОЧЕРЕДИ ЗАКРЫТ: алерт целиком на языке админ-чата
  (28e1078..6f5f120, семь коммитов, тестов 1175). Каркас
  алертов перевели волной C, а ПРИЧИНУ служебные пути отдавали готовой русской
  строкой — узбекский владелец видел половину экрана на чужом языке. Решение:
  Reason(str) в admin_texts — как строка равен русскому тексту (логи, фейки
  тестов и проверки подстрокой не тронуты), но несёт ключ шаблона с
  подстановками, и TelegramEscalation рендерит причину на язык КАЖДОГО
  получателя при отправке (у админ-чатов одной клиники языки разные, раньше
  выбрать было негде). Переведены 15 причин, адресованных клинике (просьба
  администратора, сбой подтверждения, пустой горизонт при записи и переносе,
  откаты ручных правок календаря, вытеснение записи ручным событием с
  переносом и без, два приёма на одно время, мёртвый OAuth-токен, залипший и
  восстановленный синк, лимит токенов, недоставленное напоминание,
  необработанное сообщение) + выжимка брони «Что хотел пациент» (ключи ctx_*,
  метка услуги на языке получателя; шесть ветвей заменены таблицей).
  ОСОЗНАННО НЕ ПЕРЕВЕДЕНЫ 4 системные причины (TLS-сертификат, бэкапы,
  webhook, дрейф модели): они уходят не клинике, а тому, кто обслуживает
  систему, — переводить их некому. Новые 22 строки внесены в пакет носителя
  (docs/UZ_STRINGS.md, раздел 16) — тест пакета сам поймал их отсутствие.
  ПЛЮС ИНВАРИАНТ: перевод обязан нести те же подстановки, что русский —
  рендер идёт в момент отправки, поэтому потерянная подстановка молча уносит
  данные («синк не работает циклов подряд» без числа), а лишняя роняет
  отправку KeyError уже в проде. Расхождений сейчас нет, тест проверен
  искусственной потерей {cycles} (одних имён мало: «{cycles[typo]}» для regex
  неотличим — добавлен пробный рендер каждого шаблона).
  ДВА БЛОКЕРА РЕВЬЮ ЗАКРЫТЫ:
  (1) 453fd3b — сломанный шаблон гасил алерт: опечатка в ключе или подстановке
  (а узбекские строки ещё будет править НОСИТЕЛЬ) роняла служебный путь либо
  прерывала рассылку, и ни один получатель не узнавал о сбое; у залипшего синка
  вдобавок оставался неснятым флаг «уже алертил» → рендер причины стал
  fail-open (admin_texts.render_reason: сбой в log.error, получателю ключ с
  подстановками) и форматирует ВСЕГДА, потому что plain() при пустых
  подстановках отдаёт шаблон как есть и забытый параметр уезжал владельцу
  буквальным «{cycles}»;
  (2) a7b6a88 — standalone-синк `python -m navbat.calendar` собирал нотификатор
  БЕЗ резолвера языка (супервизор и telegram/app передавали — забыть легко
  именно там, где сборка дублируется) → единственная сборка для прода
  escalation.build_escalation(), все три точки входа через неё.
  (3) 6f5f120 — перехват был неполным: «{cycles.typo}» даёт AttributeError,
  незакрытая скобка ValueError → ловим ЛЮБУЮ ошибку формата (шаблон — данные,
  не код); лишняя подстановка молча игнорировалась (уберут плейсхолдер при
  вычитке — число исчезнет из алерта незаметно) → log.warning; сбой резолвера
  языка отдавал русский без лога → log.warning; аварийный текст без фигурных
  скобок. Плюс СТРУКТУРНАЯ проверка: прямой конструктор TelegramEscalation в
  рабочем коде остался только там, где он определён — юнит фабрики возврат в
  standalone-синк не поймал бы.
  УРОК: «алерт существует, чтобы администратор узнал о сбое, а не чтобы
  красиво выглядеть» — рендер текста никогда не должен отменять доставку.
  РЕВЬЮ: пять кругов; независимым прогоном прошёл первый (три блокера, все
  закрыты), дальше Codex CLI перестал подниматься (нет `codex` в PATH под
  sandbox-профилем, один прогон вообще упал с «The 'pytest' model is not
  supported» — форвардер принял слово из промпта за флаг модели), поэтому
  остальные круги статические и помечены как таковые. Их находки проверены
  по коду и закрыты; итоговое состояние независимым прогоном НЕ подтверждено.
- 30.07.2026 (поздний вечер) СОСТЯЗАТЕЛЬНЫЙ АУДИТ НЕАУДИРОВАННОГО ПЕРИМЕТРА
  по команде «довести систему до инженерного идеала». Выбор пользователя из
  четырёх вариантов — «весь периметр одной волной». Аудит: 5 параллельных
  поисковиков (транспорт+очередь, автономный фон, арендатор+крипта, деньги
  витрины, NLU-обёртки) → 22 находки → верификатор-скептик на каждую
  (10 успели пройти до прерывания: 7 выжили, 3 опровергнуты; остальные
  проверены сессией по коду). Причина выбора: аудит всегда шёл туда, куда
  пользователь только что ткнул живьём, а ВХОДНАЯ ДВЕРЬ и АВТОНОМНЫЙ ФОН
  не смотрел никто ни разу — там живут тихие отказы, на которые никто не
  жалуется. Тестов 1206, --check 14×[OK], всё в origin/master.
  ЗАКРЫТО (каждый фикс TDD, репро-первым, проверен откатом):
  f877af4 кнопка напоминания отменяла ЧУЖУЮ запись — карта кнопок одна на
  чат и перезаписывается каждой отправкой, а напоминание висит до приёма;
  у пациента с двумя записями следующий пуш занимал номера (доказано
  сквозным тестом: отменялась не та запись) → attend:/remind_cancel:
  сырыми, префиксы сведены в RAW_CALLBACK_PREFIXES/UNNUMBERED_PREFIXES;
  794a927 вечерняя сводка: веер по админ-чатам был в одном try, отметка дня
  ставилась после всех → мёртвый чат слал сводку живым каждые 30 с до
  полуночи, а стоя первым — съедал её у всех → изоляция получателей,
  день отмечается при ≥1 доставке;
  9d7cbe1 напоминание о ПРОШЕДШЕМ приёме (простой процесса, перенос ближе
  офсета; статус после визита остаётся 'booked' навсегда) — уходило с живой
  кнопкой «Отменить», а отмена из напоминания идёт в /stats деньгами;
  9718c87 вход `python -m navbat.telegram` собирал воркер без admin_chat_id
  — админ-команды и консоль мертвы, сообщения владельца шли в пациентский
  диалог (тот же класс, что standalone-синк календаря);
  180befb /forget не трогал waitlist — «забытый» пациент получал пуши и под
  них заново создавалась строка conversation (PRIVACY.md разд. 6);
  e570a5a потоки супервизора: клейм/ack/реклейм и sync_loop.run_once ходили
  в БД вне защиты — обрыв соединения уносил поток НАВСЕГДА, процесс жив,
  апдейты копятся, docker-healthcheck зелёный (light-ветка смотрит только
  БД) → try/except с backoff + НАДЗОР supervise_threads (смерть потока =
  system_alert + остановка процесса с кодом 1);
  65aa8d9 по 1-му кругу ревью: active_by_id получил chat_id и «ещё не
  начался» (id из callback_data = от клиента: чужая запись отменялась;
  тап после начала приёма шёл в сводку как предотвращённая неявка) +
  воскрешение погашенного напоминания при переносе приёма из прошлого в
  будущее;
  0745798 матчер очереди писал услугу в conversation слепым read-modify-
  write — пациент, оформлявший другую запись, терял выбор и записывался на
  чужую услугу; кнопка слота была нумерованной → умирала после любой
  следующей отправки → сырой wl:take:<id>:<минуты эпохи>, услуга из строки
  очереди, врач подбирается при тапе; напоминания больше не создают
  conversation;
  4616634 LengthFinishReasonError и ответ без choices наследуют OpenAIError,
  но не APIError → сообщение пациента уходило в dead letter вместо мягкого
  «не понял»; retention отмечал день ДО чистки; calendar.auth печатал [OK]
  при нуле обновлённых строк (опечатка в --clinic, код Google одноразовый);
  0e9c75e _resolve_doctor сверял подстроку в обе стороны — один символ
  привязывал пациента к первому попавшемуся врачу;
  f5757e4 по 2-му кругу ревью (МОЙ РЕГРЕСС): врач подбирается при тапе,
  поэтому повторный тап по предложению занимал ВТОРОГО врача на то же
  время → проверка has_future_active + закрытие строки очереди;
  cbaa064 подтверждение отмены несёт expected_start (пока пациент читал
  вопрос, запись могла переехать), мусорный uuid не роняет обработку,
  надзор публикует имена умерших ДО остановки и не отменяет остановку из-за
  недоставленного алерта.
  РЕВЬЮ: три круга Codex, ВСЕ независимым прогоном (CLI поднялся; один
  прогон упал по лимиту сессии — перезапущен). Первый — 3 блокера (IDOR по
  чужому id, отмена начавшегося приёма, невоскресающее напоминание), все
  закрыты. Второй — 1 блокер (двойной тап по предложению) + 8 замечаний;
  закрыты блокер и четыре замечания (остальные — подтверждения, что путь
  верен). Третий — «БЛОКЕРОВ НЕТ» + 3 замечания, из них два были реальными
  следствиями моих же правок (протухший hold закрывал очередь; переспрос
  после переезда шёл про другую запись и терял источник отмены) — закрыты
  в aac5b94. КАЖДЫЙ новый тест проверен откатом фикса.
  ЖИВАЯ ПРОВЕРКА, которой тесты дать не могли: реальный Bot API принял новые
  сырые callback_data (attend: 43 байта, remind_cancel: 50, wl:take: 27,
  wl:leave: 19 — лимит 64). Драйвер live_poke/callback_limit_probe.py (НЕ
  коммитить); в админ-чат ушло одно проверочное сообщение.
  ОСОЗНАННО НЕ ЧИНИЛОСЬ (docs/PILOT_HARDENING.md T6/T7): нет задержки между
  попытками очереди (затяжной аутэйдж Telegram > ~30 с уводит сообщение в
  dead letter, но с ops-сигналом — не тихо) и reclaim_stale не отличает
  мёртвый воркер от долгого (дубль обработки после 5 минут). Оба фикса
  задевают запрос клейма — ядро порядка per-chat и дедупа, у которого есть
  конкурентные тесты; без сигнала с живого трафика трогать дороже.
  Опровергнуто верификаторами и НЕ бралось в работу: ночные пуши очереди и
  повторы раз в 6 часов (спроектированное поведение, закреплено тестами),
  dev-ключ шифрования без NAVBAT_ENC_KEY (прод-путь защищён --real и пустой
  строкой в .env), waitlist вне retention при WAITLIST_ENABLED=0
  (недокументированный рубильник, наружу ничего не течёт).
- ОЧЕРЕДЬ РАБОТ (решение пользователя 28.07: «берём то, что должны, по
  очереди»). Порядок — сверху вниз, каждый пункт по явной команде:
  1. ~~H2 CRASH-WINDOW~~ — ЗАКРЫТ 28.07 (см. запись выше).
  2. L8 персист счётчика сбоев синка — только по сигналу с пилота
     (нужна миграция; опасный режим уже прикрыт gcal_last_sync_at
     и /health).
  3. ~~Причины ВНУТРИ алертов по-русски~~ — ЗАКРЫТО 30.07 (запись ниже).
  4. Вычитка узбекского носителем (№20) — внешний гейт, пакет готов
     (docs/UZ_STRINGS.md, защищён тестом).
  5. Fine-tuning NLU (~$9, инфра И-5 готова) — гейт: данные пилота,
     не деньги; marginal product низкий, остаток дыр прикрыт FSM.
  СОСТОЯНИЕ НА 30.07: технически доступных пунктов в очереди НЕ ОСТАЛОСЬ —
  1 и 3 закрыты, 2 и 5 ждут сигнала с пилота, 4 ждёт человека-носителя.
  Из задокументированного технического долга без гейта существует только
  T3 (outbox для исходящих: уведомление о переносе не переживает падение
  процесса между коммитом и отправкой, docs/PILOT_HARDENING.md) — работа
  крупная и до живого трафика спорная, поэтому НЕ начинать без явной
  команды. Пилот Ф2, показ покупателю и деплой — СТРОГО по явной команде
  пользователя, в «следующие шаги» не включать.
- 28.07.2026 ОКРУЖЕНИЕ СЕССИИ (важно для продолжения): Docker Desktop
  падал дважды (служба WslService была DISABLED, чинится UAC-командой
  `sc config WslService start= demand`), поэтому база поднята НАТИВНЫМ
  PostgreSQL 18 на том же порту 5434: PGDATA — `%LOCALAPPDATA%\navbat-pg\
  pgdata`, старт `"C:\Program Files\PostgreSQL\18\bin\pg_ctl" -D <PGDATA>
  -l pg.log -o "-p 5434" start`. ОБЯЗАТЕЛЬНО `ALTER DATABASE navbat SET
  timezone TO 'UTC'` (уже сделано): без него 3 календарных теста красные
  на сравнении +05:00 vs +00:00. PGDATA держать ВНЕ C:\WINDOWS\TEMP —
  уборщик системного temp сносит пустые служебные каталоги кластера.
  ПОПРАВКА 30.07 (стоила часа): `pg_ctl ... start` БЕЗ `-W` ждёт готовности
  сервера, ловит таймаут инструмента, harness убивает job — постмастер
  выживает, но ЛЮБОЙ его backend падает с 0xC0000142 (DLL init failed) и
  кластер уходит в shutdown на первом же коннекте. Лечится: `-W` (не ждать),
  а надёжнее — поднимать `postgres.exe -D <PGDATA> -p 5434` ФОНОВОЙ задачей;
  снятие фоновой задачи гасит базу (за сессию случилось трижды). Из своего
  терминала пользователь запускает через `!` — в Git Bash переменная
  `$LOCALAPPDATA`, не `%LOCALAPPDATA%` (иначе `-l` получает пустой аргумент).
  Планировщик задач как обходной путь НЕ сработал.
  Telethon-сессия ЗАЛОГИНЕНА (spike_nlu/.harvest.session, аккаунт dejavuu
  @plssOG, 2FA пройдена) — живой тык в реальном Telegram доступен без
  участия пользователя; драйверы live_poke/waveb_probe.py и
  admin_waveb_probe.py (НЕ коммитить), боту нужен client.get_entity перед
  первой отправкой. Аккаунт совпадает с админ-чатом 7082498953: для
  пациентского пути временно `onboard --remove-admin`, потом вернуть, и
  ПЕРЕЗАПУСТИТЬ бота — список админ-чатов читается только при старте.
- Рабочие заметки: 11.06 ВСЯ история переписана filter-branch (автор →
  noreply-адрес GitHub пользователя для графа контрибуций; упоминания
  ассистента убраны из сообщений — В НОВЫХ КОММИТАХ Co-Authored-By
  НЕ ДОБАВЛЯТЬ, решение пользователя) → хеши коммитов ДО 11.06 в этом
  якоре устарели, искать по тексту сообщения; хеши батча П-1…П-7
  актуальны. Docker Desktop может быть выключен — стартануть и
  ждать демона; на Mac-машине пользователя создан .venv (python3.12,
  `pip install -e ".[dev]"`) — команды через `.venv/bin/python`;
  deploy/.env сгенерирован локально (gitignored);
  .recon_group_c.md в корне — рабочий артефакт, не коммитить;
  финализатор pytest теперь сам восстанавливает демо-клинику.
- Запуск с двух машин: СТРОГО один бот за раз (два процесса на один
  TG-токен = 409 Conflict — остановить второго перед стартом).
  Windows-.env полный (7 ключей, вкл. OPENAI_API_KEY и
  NAVBAT_GCAL_CLIENT_ID/SECRET); на мак .env через git НЕ едет —
  донести руками OPENAI_API_KEY + NAVBAT_GCAL_CLIENT_ID/SECRET.
  База на каждой машине своя: после git pull на маке — alembic
  upgrade head (0017–0018), onboard --demo, FAQ-поля
  (--address/--payment/--phone), --doctor <uuid> --calendar primary,
  calendar.auth (клик в браузере той машины), --check, потом --real.
  photo_2026-06-11_*.jpg в корне Windows-копии — скрины пользователя,
  не коммитить.
- На стороне пользователя (когда решит): VPS+домен, S3-хранилище,
  верификация Google-приложения, NAVBAT_OWNER_CHAT_ID. Платные прогоны
  (P4 T1/T2), глубокий узбекский (носитель), m2/m4 — гейты прежние.
- Пилот Ф2 — строго по явной команде.
- Эту секцию ОБНОВЛЯТЬ в конце каждой сессии: где остановились + следующий
  шаг. Это якорь преемственности между чатами.


## Статус проекта

- Ф0 (спайк) и Ф1 (5 инкрементов кодового ядра + button-first вход) — ЗАКРЫТЫ.
  Вся система запускается `python -m navbat`.
- СТРАТЕГИЯ v5 (решение пользователя 06.06.2026): строим до инженерных 100%
  → пилот как запуск готового продукта → продажи. Никаких «для демо сойдёт».
- Ф1.5 Production-Ready v1.0: КОДОВАЯ ЧАСТЬ ЗАКРЫТА 07.06.2026 — группы A
  (диалоговая надёжность), B (LLM-устойчивость), D.2–D.3 (приватность),
  E (продукт); 308 тестов, миграции 0001–0008, всё в origin/master.
  Детали — чекбоксы BRIEF разд. 14. D.4 (узбекские строки) ЗАКРЫТ
  07.06.2026 кросс-ревью LLM; D.1 (юрист по ПД) СНЯТ решением
  пользователя 07.06.2026. Группа C закрыта 10.06.2026 (C-1…C-7 +
  ops-hardening) — Ф1.5 ЗАКРЫТА ЦЕЛИКОМ, инженерные 100% достигнуты.
- Ключевые решения Ф1.5 (поверх Ф1):
  - escalated не вечен: /start пациентом или /release в админ-чате; счётчик
    сбоев при выходе сбрасывается;
  - «Сейчас клиника закрыта» при запросе «на сегодня» вне рабочего окна дня
    (union графиков врачей; обед ≠ закрыто); clock инжектируется
    в DialogEngine — время тестируемо;
  - выходные дни: /dayoff DD.MM [причина] и /dayopen DD.MM в админ-чате
    (предзаполненный календарь праздников ОТМЕНЁН — решение пользователя:
    многие частные клиники работают в праздники);
  - fallback-LLM: OpenAI → Gemini (nlu/fallback.py + gemini_extractor.py,
    тонкий httpx; включается наличием GEMINI_API_KEY в .env — задан).
    Аутэйдж = ProviderDownError (сеть/5xx/429), кривой JSON failover
    НЕ триггерит. Eval на узбекском ОТМЕНЁН (free tier Google = 20
    запросов/день, billing не включаем) — валидация Gemini живым пилотом,
    риск осознан; eval.py умеет gemini-* (--rpm, --source) на будущее;
  - метрика NLU-дрифта: failures/repairs в llm_usage (0007), алерт админу
    при >20% сбоев за день (≥20 запросов, NAVBAT_NLU_DRIFT_THRESHOLD);
  - промпт в БД: nlu_prompt (0008) + clinic.nlu_prompt_version (NULL =
    встроенный файл); staging = демо-клиника; смена промпта = пин + рестарт;
  - /forget <chat_id> — анонимизация пациента (записи остаются обезличенными,
    будущие НЕ отменяются); retention: чистка очереди/диалогов старше
    NAVBAT_RETENTION_DAYS=90, раз в день из цикла напоминаний;
  - дашборд денег: отмена из напоминания (actor='reminder' в аудите)
    с перезаписью слота → «предотвращено неявок + сохранено сум»
    в /stats и дайджесте.
- Команды админ-чата: /stats [дней: 7|30], /release <chat>,
  /dayoff DD.MM [причина], /dayopen DD.MM, /forget <chat>,
  /pause [причина], /resume, /llm on|off. Подсказка /release приходит
  в алерте эскалации.
- Демо-бот НАСТРОЕН и проверен: @MyCompanyDev_bot привязан к демо-клинике
  (токен в clinic.tg_bot_token_encrypted, admin_chat 7082498953, тестовый),
  `python -m navbat --check` — все [OK]. Запуск: `python -m navbat
  --reminder-offsets 4,2`.
- ВАЖНО: pytest TRUNCATE'ит ВСЮ базу, включая демо-клинику с токеном.
  Восстановление — ОДНА команда: `python -m navbat.onboard --demo`
  (токен и админ-чат подтягиваются из локального .env: NAVBAT_TG_TOKEN,
  NAVBAT_TG_ADMIN_CHAT; файл в .gitignore, у пользователя не спрашивать).


## Прогресс по инкрементам Ф1

- [x] 1. Scheduling engine + модель данных — ГОТОВ (24 теста зелёные, включая
      50-поточную гонку; postgres на :5434 — 5433 занят соседним проектом;
      буфер в выражении exclusion constraint через timezone('UTC',...) —
      timestamptz-арифметика в индексах требует IMMUTABLE; записи по врачу
      сериализованы advisory-локом — UPDATE строки в predicate exclusion
      constraint дедлочится с конкурентными INSERT, одиночный прогон теста
      этого НЕ ловит, гонять полный сьют многократно)
- [x] 2. FSM + slot-filling — ГОТОВ (102 теста зелёные, серия 8/8; всё из
      скоупа заложено: кнопки в модели Reply, бэкстоп question+date_ref→слоты
      (без услуги — сетка по checkup), прерывание вопросом = ответ + повтор шага,
      is_medical дисклеймер раз за диалог, 2 ExtractionError подряд →
      escalated (ПЕРЕСМОТРЕНО П-2а 11.06: теперь мягкий путь без эскалации),
      conversation в миграции 0002, демо `python -m navbat.demo` на фикстурах
      спайка — 410 фраз как бесплатные NLU-тесты; имя/телефон до confirm:
      телефон нормализуется к 998…, hash с clinic.salt, имя AES-256-GCM через
      NAVBAT_ENC_KEY; грабли: Windows-пайпы суют BOM в stdin демо)
- [x] 3. Channel adapter — ГОТОВ (140 тестов, серия 8/8; очередь message_queue
      в миграции 0003: ack после обработки (двухфазный клейм), дедуп по
      UNIQUE(clinic_id, update_id), per-chat порядок решает сам клейм-запрос
      (NOT EXISTS + SKIP LOCKED, advisory lock не нужен); транспорты polling и
      webhook (stdlib HTTP-сервер, secret-заголовок) за одной очередью;
      тонкий httpx-клиент Bot API без asyncio-фреймворков; callback_data
      длиннее 64 байт — кнопки нумеруются, map в conversation.context,
      устаревшая кнопка → повтор шага; greeting-дисклеймер первого контакта;
      эскалации в tg_admin_chat_id; запуск: `python -m navbat.telegram
      --clinic <uuid>`, NLU по умолчанию фикстурный — live-smoke бесплатен)
- [x] 4. Календарь — ГОТОВ (173 теста, серия 10/10; тонкий httpx-клиент GCal
      (без google-api-python-client), календарь на врача, reconciliation-sync:
      экспорт по diff time_range↔gcal_synced_range (миграция 0005, ноль чтений
      Google), импорт ручных событий как gcal_import-записи (all-day закрывает
      день), бот — истина для своих событий (ручная правка откатывается+алерт);
      конфликт: ручное вытесняет ботовскую → авто-перенос на ближайший слот
      + reslot-кнопки пациенту + эскалация, живые hold не вытесняются (ждём
      TTL); freeBusy-guard перед confirm (graceful при недоступном Google);
      онбординг python -m navbat.calendar.auth (loopback OAuth), sync-CLI
      python -m navbat.calendar; push-каналы watch — на деплое, базис —
      периодический syncToken-инкремент; грабли: stdlib HTTP-сервер обязан
      вычитать тело запроса до ответа-отказа, иначе флаки WinError 10053)
- [x] 5. Напоминания, надёжность, демо-продукт — ГОТОВ (203 теста, серия 8/8,
      CI зелёный; reminder-таблица в миграции 0006, reconciliation из
      appointment (не таймеры — переживает рестарт), кнопки «Приду»/«Отменить»
      в напоминании (отмена освобождает слот), retry 3 → failed + алерт;
      деидентификация телефонов перед LLM + имена в LLM не уходят (эвристика
      шага имени); дневной token cap (llm_usage, NAVBAT_DAILY_TOKEN_CAP) +
      rate limit >5 сообщ/10с; LLM-таймаут 8с; /stats в админ-чате + вечерний
      дайджест 21:00 (clinic.last_digest_date); супервизор `python -m navbat`
      (транспорт+воркеры+календарь+напоминания одним процессом), онбординг
      `python -m navbat.onboard` (--demo/--tg-token/--calendar/--list),
      преддемо-чеклист `python -m navbat --check`; CI: GitHub Actions,
      сьют ×3 против реального postgres)

Кодовое ядро Ф1 закрыто. Дальше: живое демо → пилот → деплой после продажи.
