# Карта готовности к продаже

Составлена 27.07.2026 прогоном, не по памяти. Сьют и сценарий гнались на
изолированном кластере PostgreSQL 18 на :5434 (Docker Desktop в тот момент не
стартовал — служба `WslService` в состоянии DISABLED, находка №22); после
починки Docker ключевые находки перепроверены на **настоящей базе проекта**
(docker-том, ревизия 0021 = head). Полный `pytest` против неё сознательно НЕ
запускался: он TRUNCATE'ит демо-клинику вместе с токеном и GCal-привязкой.

Что прогнано:

- `python -m pytest -q` → **1027 passed** (после `ALTER DATABASE navbat SET
  timezone TO 'UTC'` — нативный кластер стартовал с TZ хоста `Asia/Tashkent`,
  и 3 календарных теста падали на сравнении `+05:00` vs `+00:00`, тот же
  момент времени; docker-образ отдаёт UTC, потому в CI этого не видно);
- `python -m navbat --check` → **все `[OK]`, exit 0** — и на временном
  кластере, и на настоящей базе проекта;
- сценарий `docs/DEMO.md` пройден в `python -m navbat.demo` пошагово
  (шаги 1, 4, 5, 10 — целиком; шаги 2–3 упираются в находку №18);
- `/stats 7` отрендерен на демо-клинике живым кодом;
- поведение выходных, расписания и эскалации проверено отдельными прогонами.

Сьют зелёный и `--check` без `[FAIL]` — правило «красный сьют = находка №1»
не сработало, линзы отработаны в полном объёме.

## Находки

| # | Находка (file:line) | Линза | Что увидит покупатель | Класс | Объём | Гейт |
|---|---|---|---|---|---|---|
| 1 | `src/navbat/supervisor.py:227-228` — ненастроенный Google Calendar печатается как `[OK] Google Calendar — не настроен`; привязка врача (`doctor.gcal_calendar_id`) не проверяется вовсе | эксплуатация | чеклист весь зелёный, а шаг 9 показа (событие в календаре врача с именем и телефоном — сильнейший «вау») молча мёртв. **Проверено на настоящей базе проекта прямо сейчас:** `--check` = все `[OK]`, exit 0, при этом `gcal_refresh_token` отсутствует и календарь привязан у 0 врачей из 2 — то есть шаг 9 в текущем состоянии не пройдёт | БЛОКЕР ПОКАЗА | S | нет |
| 2 | `src/navbat/supervisor.py:157-241` — чек не смотрит `clinic.bot_paused` и `llm_enabled`, которые гейтят весь диалог (`src/navbat/telegram/worker.py:180-184,275-280`) | эксплуатация | забытый с прошлой сессии `/pause` даёт зелёный `--check` и «Запись через бота временно приостановлена» первому же сообщению покупателя | БЛОКЕР ПОКАЗА | S | нет |
| 3 | `src/navbat/stats.py:65-69` + порядок шагов `docs/DEMO.md:54-55` — шаг 10 (кнопка «Позвать администратора») выполняется до шага 11 (`/stats 7`), и эскалация попадает в счётчик периода | владелец | вы говорите «эскалаций: 0 — бот не дёргает администратора», а на экране `• эскалаций к администратору: 1`. Воспроизведено: после шага 10 рендер сводки дал ровно эту строку | БЛОКЕР ПОКАЗА | S | нет |
| 4 | `src/navbat/onboard.py:359-393` (сидер демо) — истории записей нет, `/stats` считает по факту | владелец | **Настоящая база проекта сейчас:** `/stats 7` = «записей: 0 · предотвращено неявок: 0 (≈ 0 сум) · отмен: 0 · эскалаций: 0», секции «Клиенты», «Топ врачей», «Хит-услуга» не рендерятся вовсе (пустые скрыты, `stats.py:277-295`). Денежный слайд — пустой экран под слова «управляйте клиникой по цифрам» | ОСЛАБЛЯЕТ | M | нет |
| 5 | `src/navbat/dialog/fsm.py:182-183,263-264` + `src/navbat/dialog/replies.py:203-208` — в `escalated` не работают ни меню, ни кнопки; выход только `/start` (в тексте не назван) или `/release`, иначе до retention 90 дней (`src/navbat/retention.py:38-44`) | пациент | после эскалации пациент жмёт «📅 Записаться», «💰 Цены», пишет «здравствуйте» — на всё один ответ «Передаю администратору». Проверено прогоном: 4 разных ввода, 4 одинаковых ответа. Из тупика нет выхода одним тапом | ОСЛАБЛЯЕТ | S | нет |
| 6 | ✅ ЗАКРЫТО (`77c5b67`). `src/navbat/telegram/admin_console.py:762-779` + `src/navbat/onboard.py:192-202` — «Свой график» пишет `working_intervals` целиком | админ | владелец задал шаблон «Пн–Пт 09–18», затем «Свой график → Сб 09:00-13:00» — проверено: в базе осталась ТОЛЬКО суббота, пн–пт стёрты без предупреждения. Типовой график «будни длинные, суббота короткая» кнопками не заводится | ОСЛАБЛЯЕТ | M | нет |
| 7 | ✅ ЗАКРЫТО (`47b4307`). `src/navbat/telegram/worker.py:380-393`, `src/navbat/telegram/admin_console.py:833-855`, `src/navbat/reminders.py:92-113` — закрытие дня не трогает уже созданные записи | админ | закрыли день на праздник — проверено: запись осталась `booked`, напоминание запланировано, пациент получит «🔔 Ждём вас!» на день, когда клиника закрыта. Ни предупреждения владельцу («на этот день N записей»), ни письма пациенту | ОСЛАБЛЯЕТ | M | решение владельца |
| 8 | `src/navbat/demo.py:65,72` — рендерятся только плоские `reply.buttons`; месячная сетка, day-view и «🔔 Сообщить, когда освободится» живут в `button_rows` (`src/navbat/dialog/calendar_flow.py:75,161,183-189`) | эксплуатация | репетиция показа в консоли обрывается на шаге 2–3: «📅 Выбрать дату» → «📅 Июль 2026 / • — есть свободное время» и ни одной кнопки. Прогнать сценарий целиком можно только в живом Telegram | ОСЛАБЛЯЕТ | S | нет |
| 9 | `src/navbat/dialog/calendar_flow.py:179` → `src/navbat/telegram/escalation.py:79-82` — 🟡 FYI «нет слотов» уходит тем же `notify`, что и настоящая эскалация | владелец | в админ-чате приходит «Эскалация: чат 123 / Причина: нет слотов на 2 недели вперёд / Снять: /release 123», хотя продаём «это не эскалация, а FYI» и `/stats` в этот момент честно показывает 0 | ОСЛАБЛЯЕТ | S | нет |
| 10 | `src/navbat/telegram/worker.py:86-96` → `src/navbat/telegram/escalation.py:90-105` — системные алерты веером идут во ВСЕ админ-чаты клиники | эксплуатация | если при клиенте что-то упадёт, он прочитает в том же чате «⚠ Системный алерт / апдейт 4711 в dead letter: <текст исключения>» — техника наружу в худший момент | ОСЛАБЛЯЕТ | S | нет |
| 11 | `src/navbat/telegram/admin_console.py:559-562` + `src/navbat/onboard.py:240-257` — деактивация врача молчит о его будущих записях | админ | «⛔ Скрыть» → «⛔ Врач скрыт». Владелец считает, что убрал врача, а записи к нему остаются и пациенты придут | ОСЛАБЛЯЕТ | S | нет |
| 12 | `src/navbat/dialog/booking_flow.py:138-153` — на шаге телефона только кнопка контакта, кнопки «позвать администратора» нет (фраза работает, но пациент её не знает) | пациент | недоверчивый или пожилой пациент, не желающий отдавать номер, получает «нажмите кнопку ниже» на любой текст и уходит. Ручной ввод отключён сознательно | ОСЛАБЛЯЕТ | S | **НЕ БЕРЁМ** — решение владельца 27.07.2026: номер только кнопкой |
| 13 | `src/navbat/dialog/replies.py:354-357` — голосовое сообщение получает «Пока я понимаю только текст» | пациент | в Ташкенте голосовые — обычный способ письма у старшей аудитории; покупатель спросит про них почти наверняка | ОСЛАБЛЯЕТ | L | **СНЯТО** — решение владельца 27.07.2026 (подтверждает M6): голос не нужен, тему не поднимать |
| 14 | ✅ ЗАКРЫТО (`3d5196d`). `src/navbat/telegram/worker.py:172-179,213-219` — админ-чат это чистая консоль, пациентский диалог из него недоступен | админ | владелец на встрече берёт свой телефон «дайте попробую» и попадает в админ-меню, а не в бота. Нужен второй аккаунт и объяснение, почему так | ОСЛАБЛЯЕТ | S | решение владельца |
| 15 | `src/navbat/stats.py:89-99` — «предотвращено неявок» растёт только на отмене из напоминания (`actor='reminder'`) | владелец | на шаге 8 естественнее нажать «✓ Приду» — и денежная строка останется «0 (≈ 0 сум)» ровно там, где вы говорите «одна спасённая запись окупает месяц» | ОСЛАБЛЯЕТ | S | нет |
| 16 | ✅ ЗАКРЫТО (`42dd1bf`, `823a82b`). `src/navbat/telegram/admin_console.py:24-32` и весь рендер — консоль только на русском | админ | узбекоязычный администратор клиники управляет ботом на русском, пока сам бот гордо говорит по-узбекски | ОСЛАБЛЯЕТ | M | носитель-uz |
| 17 | `docs/DEPLOY.md:127-131` — в testing-режиме OAuth refresh-токен Google живёт 7 дней; после любого полного `pytest` привязка врача к календарю теряется вместе с базой | эксплуатация | календарная часть показа тихо умирает через неделю после авторизации; поймать это можно только повторным `--check` (он проверяет живость токена, но не привязку — см. №1) | ОСЛАБЛЯЕТ | S | решение владельца |
| 18 | `src/navbat/telegram/admin_console.py:310-316,567-573` — «🗑 Удалить совсем» выполняется по первому тапу | админ | подтверждения нет, отката нет. Риск ограничен (кнопка живёт только у скрытой сущности без ссылок), но «а если я случайно» — вопрос владельца на первом же занятии | КОСМЕТИКА | S | нет |
| 19 | `src/navbat/dialog/replies.py:98-103` — «напишите "позовите администратора"» текстом, хотя во всех соседних тупиках стоит кнопка | пациент | единственное место, где пациента просят набрать фразу вручную вместо тапа | КОСМЕТИКА | S | нет |
| 20 | ⚠️ ПАКЕТ ГОТОВ (`b7675a9`), вычитка за человеком. `src/navbat/dialog/replies.py` целиком — узбекские строки проверены кросс-ревью LLM, не носителем (BRIEF разд. 14.D) | пациент | владелец-узбек читает интерфейс на своём языке и может поморщиться на формулировке; сам текст без носителя не править | ОСЛАБЛЯЕТ | M | носитель-uz |
| 21 | ✅ ЗАКРЫТО (`699fa2c`, `docs/OFFER.md`). Репозиторий: коммерческого одностраничника нет, цифры живут в `BRIEF.md:19` (для разработчика) | владелец | на вопросы «сколько стоит», «что входит», «за сколько запустите», «где лежат данные пациентов» ответы есть в голове, но нечего оставить на столе после встречи | ОСЛАБЛЯЕТ | S | решение владельца |
| 22 | `docker-compose.yml:1-20` + `docs/DEMO.md:10-19` — вся демо-инфраструктура держится на Docker Desktop ноутбука | эксплуатация | сегодня Docker не поднялся вовсе (`WslService` = DISABLED, потребовался UAC; к концу сессии починен) — в день встречи такое означает «показывать нечего». Обходной путь проверен в этой сессии: нативный PostgreSQL 18 на том же порту 5434 поднимает всю систему без Docker | БЛОКЕР ПОКАЗА | S | нет |

## Статус на 28.07.2026

Закрыты все 20 взятых в работу пунктов, кроме вычитки носителем (№20 —
внешний гейт). Всё в origin/master; сьют 1129 зелёный, `--check` зелёный.

| Волна | Находки | Статус |
|---|---|---|
| A — блокеры показа | №1, №2, №3, №22 | ✅ закрыты (`869c2c4`, `20befe6`) |
| B — мелкий код | №5, №8, №9, №10, №11, №15, №17, №18, №19 | ✅ закрыты (`b6d6a92`…`8ba2a4f`) |
| C — объёмное | №4 (`b305c15`…`79a3cb9`), №6 (`77c5b67`), №7 (`47b4307`), №16 (`42dd1bf`, `823a82b`), №21 (`699fa2c`) | ✅ закрыты |
| D — по решению владельца | №14 (`3d5196d`), №20 (`b7675a9`, частично) | ⚠️ см. ниже |
| Сняты решением владельца | №12, №13 | — не делаем |

**№20 остаётся открытым по существу.** Пакет для вычитки
(`docs/UZ_STRINGS.md`) догнал код: в нём все пациентские строки, вся
админ-консоль и превью, а тест не даёт добавить строку в бота мимо
документа. Сама вычитка живым носителем — вне кода и ждёт человека.

**Долг закрыт:** ре-ревью сидера демо-истории прошло и дало «не выпускать»
с двумя блокерами, оба исправлены (`bf998d7`): сводка за сегодня считает
работу бота, а не время приёма, и синтетика больше не уходит в Google
Calendar. Ревью волны C нашло ещё пять пунктов, включая регресс от этой же
правки (потеря ручного события врача) — все закрыты (`79a3cb9`…`823a82b`).

Что независимое ревью поймало в этой волне: узбекский обрывался на сводке,
паузе и слэш-командах (то есть там, где владелец проводит больше всего
времени); выбор дней расписания применялся к чужому врачу; неделя из
пустых смен проходила валидацию; демо-запись съедала ручное событие
календаря. Все — с воспроизведением по коду.

Что независимое ревью поймало в этой работе (и что исправлено): счётчик
привязок календаря расходился с реальными потребителями; сужение системных
алертов отрезало клинику от операционных сигналов; подтверждение удаления
проверяло статус не атомарно с самим удалением; кнопка выхода из заморозки
разрешалась в «attend» после перезаписи map кнопок; сидер сеял активную
очередь ожидания, будившую матчер, и мог налить синтетику живой клинике.

Что дал живой Telegram (то, чего не дали тесты): «📊 Статистика» открывает
сводку за СЕГОДНЯ — пустой день снова показывал покупателю нули (исправлено);
список админ-чатов читается только при старте процесса, `--add-admin` на лету
не подхватывается (занести в онбординг-руководство).

## План закрытия

Решение владельца 27.07.2026: закрываем **все находки, кроме №12 и №13**
(обе сняты явным решением — см. гейт в таблице). Итого 20 пунктов, волнами:

- **A. Блокеры показа (S):** №1, №2, №3, №22.
- **B. Мелкий код (S):** №5, №8, №9, №10, №11, №15, №17, №18, №19.
- **C. Объёмное (M):** №4, №6, №7, №16, №21.
- **D. По решению владельца:** №14 (превью «глазами пациента»), №20
  (узбекский; черновик — кросс-ревью LLM, финальная вычитка носителем).

Допущения, объявленные до начала работы: по №7 закрытие дня **предупреждает**
владельца о существующих записях и ничего не отменяет само; по №10 системные
алерты уходят владельцу системы, клинике остаётся только адресованное ей.

## ТОП-5 в порядке закрытия

1. **№1 + №2 — расширить `--check` (привязка календаря, `bot_paused`, `llm_enabled`).**
   Единственная защита показа сейчас врёт: сегодня чеклист был весь зелёный при
   мёртвом календаре — одна строка кода закрывает целый класс «показ умер молча».
2. **№3 — переставить шаги DEMO.md (эскалация после `/stats`) или сбрасывать демо перед встречей.**
   Показ в текущем порядке своими руками опровергает главный тезис продажи прямо
   на денежном слайде; правится редактурой сценария, не кодом.
3. **№4 — сидер демо-истории для `/stats`.**
   Витрина «управляйте клиникой по цифрам» — то, за что платят ежемесячно;
   на пустой базе она выглядит как счётчик из одной записи.
4. **№5 — выход из `escalated` одним тапом.**
   Единственный подтверждённый тупик пациента: меню на экране, кнопки мертвы,
   а обещание «до человека всегда один тап» ломается ровно после эскалации.
5. **№6 — расписание по дням в консоли (или предупреждение о перезаписи).**
   Первое, что владелец попросит завести — свой реальный график; сейчас второй
   проход молча стирает остальные дни, и это видно на первом же занятии.
